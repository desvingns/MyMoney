name = "mp-intake"
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
description = "Synthesize MyMoney MP Dev SPEC JSON from user requests and project context."
sandbox_mode = "workspace-write"

developer_instructions = """
Read AGENTS.md, then read the canonical Claude agent body at C:/Users/Admin/.claude/plugins/cache/mobile-pipeline/mp-dev/1.12.0/agents/mp-intake.md.
Read .claude/mp/extras/mp-token-budget.md, then read .claude/mp/config.json and .claude/mp/extras/mp-intake.md if it exists.
Interpret Claude tool terminology as equivalent Codex file and shell operations.
Return only the required SPEC JSON payload. Do not spawn descendant agents.
"""

# Grill ledger — dashboard «Neon Ring» (feature)

## Resolved decisions
- D1 (root): свечение неонового кольца НЕ обрезается сверху/снизу — заметка заказчика [confirmed]
- D2 (root): «Остаток» в центре динамически ужимается, чтобы не касаться внутреннего края кольца — заметка заказчика [confirmed]
- D3 (branch): кнопки −/+ на 15% крупнее (90dp→104dp) — заметка заказчика [confirmed]
- D4 (branch): список плиток категорий скроллится при переполнении — заметка заказчика [confirmed]
- D5 (root): всё прочее — один в один с макетом — заметка заказчика [confirmed]
- D6 (root): кольцо = единая градиентная дуга без сегментов; заполнение = расход ÷ доход за период (clamp 0..1; доход 0 → пустой трек) — ответ Q1 [confirmed]
- D7 (root): центр = net периода (доход − расход), округление до целого; отдельной панели баланса нет — ответ Q2 [confirmed]
- D8 (root): неон-палитра заменяет палитру ВСЕГО приложения — ответ Q3 [confirmed]
- D9 (branch): плитки = только расходы, по убыванию, полоска = доля от расхода, тап → операции категории, без «<2% Другое» — ответ Q4 [confirmed]

## Surfaced holes
- H1: перекрас всего приложения ломает контраст на не-тёмных экранах (формы/список/настройки/словари) → открыто как риск R1 (перекрашиваем, не редизайним; вычит контраста вручную)
- H2: ThemeMode System/Light/Dark теряет смысл при единой неон-палитре → решено: обе ветки → неон, переключатель пока не удалять (assumption A1)
- H3: уход от Monefy-доната убирает тап по сектору/иконки на кольце → принято (drill-down переезжает в плитки); UI-тесты переписываются в 06 (R2)

## Open questions (deferred / assumptions)
- A1: ThemeMode схлопывается в неон (обе ветки) — рекоменд. минимальное изменение; согласовать при реализации 01
- A2: суммы на плитках/в пилюле тоже целые (под макет) — рекоменд.; легко вернуть копейки
- A3: точные значения градиента/свечения/радиусов — сверять с рендером HTML на этапе 06 (--fit)

## Out of scope (explicit)
- Layout-редизайн не-дашборд экранов (только перекрас палитрой)
- Изменения поведения экранов ввода операции / списка / настроек / словарей
- Изменения домена/БД (баланс считается прежним BalanceCalculator; миграций нет)
- Кликабельность кольца и пилюли доход/расход (display-only)

name = "mp-reviewer-android"
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
description = "Review MyMoney Android MP Dev changes for architecture, TDD, and project-rule violations."
sandbox_mode = "read-only"

developer_instructions = """
Read AGENTS.md, then read the canonical Claude agent body at C:/Users/Admin/.claude/plugins/cache/mobile-pipeline/mp-dev/1.14.0/agents/mp-reviewer-android.md.
Read .claude/mp/extras/mp-token-budget.md, then read .claude/mp/config.json and .claude/mp/extras/mp-reviewer-android.md if it exists; the extras add MyMoney-specific checks.
This LLM role is the fallback path after deterministic reviewer script failure or unsupported checks; keep the review scoped to the provided context capsule and changed files.
If Bash is unavailable, perform the review directly instead of invoking C:/Users/Admin/.claude/plugins/cache/mobile-pipeline/mp-dev/1.14.0/scripts/mp-reviewer-android.sh.
Return only the required JSON payload. Do not modify files or spawn descendant agents.
"""

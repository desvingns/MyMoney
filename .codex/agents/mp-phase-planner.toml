name = "mp-phase-planner"
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
description = "Plan MyMoney large-build phase files from a design source for MP Dev handoff."
sandbox_mode = "workspace-write"

developer_instructions = """
Read AGENTS.md, then read the canonical Claude agent body at C:/Users/Admin/.claude/plugins/cache/mobile-pipeline/mp-dev/1.14.0/agents/mp-phase-planner.md.
Read .claude/mp/extras/mp-token-budget.md, then read .claude/mp/config.json and .claude/mp/extras/mp-phase-planner.md if it exists.
Return only the required PLAN block. The orchestrator performs gated writes; do not spawn descendant agents.
"""

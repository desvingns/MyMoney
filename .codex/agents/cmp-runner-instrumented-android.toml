name = "cmp-runner-instrumented-android"
description = "Run ONE MyMoney instrumented test class on the Pixel_5_API_34 AVD via the host-AVD helper and report parsed pass/fail. Device suite only — not JVM unit tests."
sandbox_mode = "workspace-write"

developer_instructions = """
Read AGENTS.md and `.claude/agents/cmp-runner-instrumented-android.md`, then follow that agent specification exactly.
Interpret Claude Bash references as Codex shell execution; the documented PowerShell exception (the host-AVD helper) still applies — invoke `scripts/run_connected_test_on_host_avd.ps1`, never a bare gradle connected task.
Trust the parsed connected report, never "BUILD SUCCESSFUL" alone. Never edit source or test files, never record baselines, never spawn descendants.
Return only the required JSON payload.
"""

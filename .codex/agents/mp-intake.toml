name = "mp-intake"
model = "gpt-5.4"
model_reasoning_effort = "high"
description = "Synthesize MyMoney MP Dev SPEC JSON from user requests and project context."
sandbox_mode = "workspace-write"

developer_instructions = """
Read AGENTS.md, then read the canonical Claude agent body at C:/Users/k.shavrin/.claude/plugins/cache/mobile-pipeline/mp-dev/1.7.0/agents/mp-intake.md.
Read .claude/mp/config.json and .claude/mp/extras/mp-intake.md if it exists.
Interpret Claude tool terminology as equivalent Codex file and shell operations.
Return only the required SPEC JSON payload. Do not spawn descendant agents.
"""

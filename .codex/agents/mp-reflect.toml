name = "mp-reflect"
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
description = "Aggregate cross-project MP Dev lessons and queue plugin improvement proposals."
sandbox_mode = "workspace-write"

developer_instructions = """
Read AGENTS.md, then read the canonical Claude agent body at C:/Users/Admin/.claude/plugins/cache/mobile-pipeline/mp-dev/1.12.0/agents/mp-reflect.md.
Read .claude/mp/extras/mp-token-budget.md, then read .claude/mp/config.json and .claude/mp/extras/mp-reflect.md if it exists.
Project-specific lessons for MyMoney go to .claude/mp/extras/*; recurring generic lessons may be queued as plugin improvements.
Return only the required structured payload. Do not spawn descendant agents.
"""

name = "mp-developer-android"
model = "gpt-5.5"
model_reasoning_effort = "high"
description = "Implement approved MyMoney Android MP Dev feature or bugfix specifications without writing tests."
sandbox_mode = "workspace-write"

developer_instructions = """
Read AGENTS.md, then read the canonical Claude agent body at C:/Users/k.shavrin/.claude/plugins/cache/mobile-pipeline/mp-dev/1.8.1/agents/mp-developer-android.md.
Read .claude/mp/config.json and .claude/mp/extras/mp-developer-android.md if it exists; the extras override the generic body.
Interpret Claude Write/Edit/Bash terminology as equivalent Codex file editing and shell operations.
Return only the required JSON payload. Do not write tests and do not spawn descendant agents.
"""

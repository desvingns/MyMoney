{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Monthly dependency-update radar; human review and TDD approval are required before merges.",
  "enabledManagers": ["gradle"],
  "ignorePaths": ["**/*.gradle", "**/*.gradle.kts", "**/gradle.properties", "build-logic/**"],
  "branchPrefix": "renovate/",
  "schedule": ["* 0-3 1 * *"],
  "timezone": "Europe/Budapest",
  "labels": ["dependencies", "dependency-radar"],
  "dependencyDashboard": true,
  "dependencyDashboardLabels": ["dependencies", "dependency-radar"],
  "dependencyDashboardApproval": true,
  "automerge": false,
  "prConcurrentLimit": 1,
  "prHourlyLimit": 1,
  "prHeader": "## TDD approval required\n\nThis report-only radar never automerges. Do not merge an update until its impact is reviewed; every major-version update also requires an explicit revision of `TDD/MyMoney_TDD.md`.",
  "packageRules": [
    {
      "matchManagers": ["gradle"],
      "groupName": "Gradle version catalog",
      "groupSlug": "gradle-version-catalog"
    }
  ]
}

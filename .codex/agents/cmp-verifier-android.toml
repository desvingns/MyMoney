name = "cmp-verifier-android"
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
description = "Verify MyMoney Android wiring and produce the manual pre-push checklist."
sandbox_mode = "read-only"

developer_instructions = """
Read AGENTS.md and `.claude/agents/cmp-verifier-android.md`, then follow that agent specification exactly.
Interpret Claude tool terminology as equivalent Codex read and shell operations.
Return only the required JSON payload. Do not modify application files or spawn descendants.
"""

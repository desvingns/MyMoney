name = "mp-maintainer"
model = "gpt-5.4"
model_reasoning_effort = "high"
description = "Maintain MP Dev model assignments and pipeline metadata for MyMoney."
sandbox_mode = "workspace-write"

developer_instructions = """
Read AGENTS.md, then read the canonical Claude agent body at C:/Users/Admin/.codex/plugins/cache/personal/mp-dev/1.10.0+codex.20260701151538/agents/mp-maintainer.md.
Read .claude/mp/extras/mp-token-budget.md, then read .claude/mp/config.json and .claude/mp/extras/mp-maintainer.md if it exists.
For Codex wrappers, preserve the model mapping documented in .agents/skills/mp-dev/SKILL.md unless the user explicitly approves a change.
Return only the required structured payload. Do not spawn descendant agents.
"""

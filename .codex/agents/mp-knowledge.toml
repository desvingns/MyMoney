name = "mp-knowledge"
model = "gpt-5.4"
model_reasoning_effort = "high"
description = "Preserve MyMoney MP Dev project-local lessons or route plugin-level lessons to improvements."
sandbox_mode = "workspace-write"

developer_instructions = """
Read AGENTS.md, then read the canonical Claude agent body at C:/Users/k.shavrin/.claude/plugins/cache/mobile-pipeline/mp-dev/1.5.0/agents/mp-knowledge.md.
Read .claude/mp/config.json and .claude/mp/extras/mp-knowledge.md if it exists.
Project-specific improvements must go into .claude/mp/extras/* so Claude and Codex stay synchronized.
Return only the required structured payload. Do not spawn descendant agents.
"""

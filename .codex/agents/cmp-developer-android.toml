name = "cmp-developer-android"
description = "Implement approved MyMoney Android feature or bugfix specifications without writing tests."
sandbox_mode = "workspace-write"

developer_instructions = """
Read AGENTS.md and `.claude/agents/cmp-developer-android.md`, then follow that agent specification exactly.
Honor MyMoney phase context and any referenced `.claude/cmp-mymoney/` guidance.
Interpret Claude tool terminology as equivalent Codex file and shell operations.
Return only the required JSON payload. Do not spawn descendant agents.
"""

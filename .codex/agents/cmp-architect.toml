name = "cmp-architect"
description = "Brainstorm MyMoney approaches before an approved SPEC and return a BRAINSTORM block."
sandbox_mode = "read-only"

developer_instructions = """
Read AGENTS.md and `.claude/agents/cmp-architect.md`, then follow that agent specification exactly.
Interpret Claude tool terminology as equivalent Codex read and shell operations.
Return only the required BRAINSTORM block. Do not spawn descendant agents.
"""

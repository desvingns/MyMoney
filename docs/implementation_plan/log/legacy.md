# Legacy / undated current-state archive

> Entries below are verbatim moves from `docs/implementation_plan/PROGRESS.md` Current state during the 2026-07-09 token-budget split.
> Keep appending new history to the matching monthly file when compacting the head again.

---

- **Active phase:** none — all implementation_plan phases `PHASE_01`–`PHASE_15` are **done**.
- **Last session:** 2026-05-26 — **PHASE_15 in progress**, continued via native `$cmp` plus API 34 device verification. The existing host-capable gates remain green (655 unit tests/0 failed, `lintDebug`, debug/release APK builds, instrumentation/macrobenchmark assembly, EN/RU parity, audio/CSV integrity; R8 release APK **12.35 MB**). A `Pixel_5_API_34` AVD is now reachable from Codex; after disabling unintended Sentry provider auto-init, application startup and connected tests are green: `:app` 3/3, `:core:designsystem` 3/3, `:core:database` 20/20, `:core:datastore` 5/5. The accumulated PHASE_14–15 checkpoint, verified device/Codex setup, and accepted history rewrite were published to `origin/main` on 2026-05-26. Baseline Profile generation, the full `MainActivity` critical path, Worker instrumentation and clean-emulator R8 walk remain open; PHASE_15 is not done. See `phases/PHASE_15_*.md` → "Notes for next session".
- **(prior) PHASE_14:** completed 2026-05-26 (Biometric lock S16 + WorkManager) across **4 `/cmp --phase` SPEC pipelines** (+ nav-wiring follow-up 2b), each green Developer→Reviewer→Tester→Runner→Verifier. Shipped: S16 biometric setup + 4-digit PIN (PBKDF2-SHA256) `60cfb4a`; runtime lock overlay + LockController per **AS-5** (overlay is NOT a nav destination — rendered in MainActivity's Box) `c2d2c0f`, S16 wired into Settings via `Destinations.LOCK_SCREEN` `a848114`; **RecurringWorker** (silent per AS-11 — logic in `GenerateDueRecurringUseCase`) + **PruneDeletedWorker** (30-day) + **BackupRotationWorker** + daily `WorkScheduler` (KEEP, from `MyMoneyApp.onCreate`) `bc9c308`; **ObserveBudgetAlertsUseCase** + `DomainEvent.BudgetAlert` + `TransactionRepository.observeAll()` `a8dd486` (tests `74f2462`/`91c43d9`/`4d0a662`/`1d9bbd2`). Done-criteria green at HEAD `1d9bbd2`: `:feature:lockscreen:assembleDebug` + `:app:assembleDebug` + **148 unit tests** (lockscreen 46 / core:sync 58 / core:domain 44) 0-failed. Notable: biometric lib pinned to **androidx.biometric 1.1.0** (the `1.2.0-alpha07` named in the phase file does not exist on Google Maven). Deferred to PHASE_15: WorkManager + Compose-UI + BiometricPrompt instrumentation (JVM seams unit-tested instead), dashboard budget-alert chip (US-26 AC2/AC3 — consumes `ObserveBudgetAlertsUseCase`), `:feature:settings` `values-ru`. Per-SPEC Verifier manual QA checklists captured. See `phases/PHASE_14_*.md` → "Notes for next session". **Commits LOCAL — not pushed (Decision 2; now covers PHASE_11–14).** ⚠ Working tree also contains unrelated CMP-framework edits (`.claude/*`, `.gitignore`, `.agents/`/`.codex/`/`AGENTS.md`) — left uncommitted, NOT PHASE_14 output.
- **(prior) PHASE_13:** completed 2026-05-24 (all tasks ticked) across 7 `/cmp --phase` pipeline passes, each green Developer→Reviewer→Tester→Runner→Verifier. Cloud sync (S17) + Sentry observability + Firebase Remote Config — shipped as a fully-built, unit-tested (fakes) skeleton that is **DevOps-gated OFF** until external creds land. Slices: foundation+RC `2920b85`, DropboxRepository `90872c5`, GoogleDriveRepository `d1002a1`, SnapshotSyncRepository orchestrator `96062f6`, SyncWorker+WorkManager/Hilt infra `639c0d7`, CloudSyncViewModel+interfaces+EN/RU strings `2467058`, CloudSyncScreen+ConflictDialog+nav+S14 `5114e3d` (tests `93d01a6`/`e261677`/`6504645`/`119b058`/`e74e8e1`/`df16614`). Done-criteria green at HEAD: `:app:assembleDebug` + `:core:sync`/`:feature:cloudsync`/`:feature:settings`:`testDebugUnitTest` + `:core:domain:test` (0 failed). RC defaults `dropbox/gdrive_sync_enabled=false` → S17 shows "Not connected", Connect disabled, no eager network. Notable: a self-recursive test-fixture `getCacheDir()` (NOT a production bug — proven byte-correct via javap) caught+fixed in 4a; a pre-existing `:app` META-INF/INDEX.LIST collision (from the cloud SDKs) + a `:feature:settings` `FakeBackupRepository` compile regression (from the 4a `BackupRepository` iface change) both fixed. Deferred: live cloud round-trips (OQ-2/3), WorkManager+Compose-UI instrumentation (PHASE_15), on-device QA (Verifier manual checklists captured per slice). See `phases/PHASE_13_*.md` → "Notes for next session". **Commits LOCAL — not pushed (Decision 2; now covers PHASE_11+12+13).**
- **(prior) PHASE_12:** completed 2026-05-24 across 5 slices — **S15 Theme** (`MyMoneyTheme(themeMode)` overload + `ThemeMode` enum in `:core:ui`; `MainActivity` observes `AppThemeViewModel` → live re-theme, no restart; commit `f065666`), **S19 Language** (`AppLocaleController` wraps `AppCompatDelegate.setApplicationLocales` so the VM stays JVM-testable; `MainActivity`→`AppCompatActivity` + `Theme.MyMoney` reparented `Theme.AppCompat.DayNight.NoActionBar`; `locales_config.xml` en/ru + manifest `localeConfig`; commit `815d4b7`), **S20 About/Help** (WebView JS-off loading bundled `assets/{privacy_policy,help}_<lang>.html` via `getApplicationLocales()`; OSS-licenses Gradle plugin in `:app` + `play-services-oss-licenses` 17.1.0; commit `26dc07a`), **S18 Backup** (SAF `OpenDocumentTree` export + `OpenDocument` import closing/overwriting `monefy.db`; OQ-8 keep-newest-3 as a pure `backupsToDelete()`; `BackupRepository` interface in `:core:domain` + impl in `:core:database` per Decision 3, VM on the domain interface; `Process.killProcess` in presentation; commit `003ec46`), **S14 Settings root** (grouped sections, current-selection trailing labels, disabled App-lock/Sync placeholders, inline Sound/Haptic switches persisting immediately, right-drawer `SettingsClicked`→`NavigateSettings`→`Destinations.SETTINGS`; commit `395adf9`). Tests committed separately (`0c8c601`/`105e1a9`/`08f2969`). Done-criteria green at HEAD: `:feature:settings:testDebugUnitTest` + `:core:domain:test` + `:app:testDebugUnitTest` (0 failed) + `:feature:settings:assembleDebug` + `:app:assembleDebug`. Reviewer + Verifier green every slice. Deferred to PHASE_15/device: live Compose-UI tests (JVM placeholders pin every screen contract — artifacts absent offline), device manual QA (theme/language live-switch, backup export/import round-trip, WebView render, OSS screen, per-app locale chip — manual checklists captured per slice), RU strings for all new keys, CSV export/import + factory-reset (TDD §4.17 AC4/AC5), an `assetSuffix()` pure seam for unit-testability. **Commits are local — not pushed (Decision 2 still pending user confirm; now covers PHASE_11 + PHASE_12).**
- **Next action:** implementation_plan is complete. Release/v1.1 readiness can resume from the deferred gates above when external credentials, release signing, and hosted CI policy are available.
- **Blockers:** NONE for implementation_plan. Release blockers remain external/deferred: Sentry DSN, cloud credentials, Firebase/CI secret policy, release keystore, hosted CI green run, and performance-budget tuning.

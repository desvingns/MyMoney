# Session log archive - 2026-06

> Entries below are verbatim moves from `docs/implementation_plan/PROGRESS.md` Current state during the 2026-07-09 token-budget split.
> Keep appending new history to the matching monthly file when compacting the head again.

---

- **2026-06-26 (Codex MP `--feature --next`, completed locally):** Completed SPEC `operations-journal-sync-07-pull-to-refresh-and-sync-status-ui` and closed the `operations-journal-sync` epic using native MP subagents. Restored the stashed WIP (`journal-sync-07` plus Aurora/theme test tweaks), merged the dashboard UI-test overlap, added journal-sync status UI/test coverage, fixed the androidTest `OperationDao` binding, and hardened `CloudSyncViewModel` so folderId/settings/peer-status failures report to Sentry and surface localized banners while coroutine cancellation still propagates. Commits: `f9db31f1`, `ffb3be4c`, `2fbf4d3b`; restored Aurora/theme WIP commit: `a261c701`. SPEC 07 and the epic overview moved to `.claude/specs/done/`; SPEC 02 implementation links were filled from existing commits. Verification: `mp-verifier-android` pass; `mp-reviewer-android` pass after failure-path repair; focused `:feature:cloudsync:testDebugUnitTest --tests com.kshavrin.mymoney.feature.cloudsync.CloudSyncViewModelTest`, `:feature:cloudsync:ktlintCheck`, `:feature:dashboard:ktlintCheck`, `:app:compileDebugAndroidTestKotlin`, `:app:assembleDebug`, `:app:ktlintCheck`, and `:core:ui:ktlintCheck` passed after the final repair; earlier connected `CloudSyncContentUiTest` 8/8 and `DashboardContentUiTest` 63/63 passed on `Pixel_5_API_34`. APK rebuilt at `app/build/outputs/apk/debug/app-debug.apk`. No push was performed.
- **2026-06-26 (Codex MP `--feature --next`, completed locally):** Completed SPEC `operations-journal-sync-03-op-emission-dual-write` using native MP subagents. Added dual-write operation journaling for transaction/category/account repository mutations: user upsert/archive/delete/restore/default changes now generate stable UUID/deviceId/updatedAt metadata and write `op_journal` `Upsert`/`Delete` entries in the same Room transaction via `TransactionRunner`; added shared JSON `OperationPayloadCodec` snapshots with UUID FK references; added suspend DataStore-backed `DeviceIdProviderImpl`, Hilt binding, and AppSettings reset preservation for the install UUID. Reviewer/verifier issues found and fixed: missing account `setDefault` and transaction `restore` journal writes, blocking `DeviceIdProvider` access, and `runBlocking` in `ImportFocusColdStartRegressionTest`. Commits: `3eddec02`, `3c96c5eb`, `16c031e0`, `30ade859`, `6dc7b266`, `bbaf3bfb`. SPEC moved to `.claude/specs/done/`; next runnable backlog item is `operations-journal-sync-04-journal-apply-engine`. Verification: `mp-reviewer-android` pass after repairs; `:core:database:testDebugUnitTest`, `:core:datastore:testDebugUnitTest`, `:core:database:compileDebugAndroidTestKotlin`, `:core:database:ktlintCheck`, `:core:datastore:ktlintCheck`, and `:app:compileDebugAndroidTestKotlin` passed; connected `TransactionRepositoryImplDualWriteTest` 5/5, `CategoryRepositoryImplDualWriteTest` 1/1, and `AccountRepositoryImplDualWriteTest` 1/1 passed on `Pixel_5_API_34` through the host-AVD helper with required post-run pauses. No push was performed.
- **2026-06-26 (Codex MP `--feature --next`, completed locally):** Completed SPEC `operations-journal-sync-01-operation-model-and-merge` using native MP subagents. Added pure `:core:domain` journal foundations: `Operation`, `EntityKind`, `OpType`, `DeviceIdProvider`, and deterministic `OperationMerger.resolve(...)` with op-id deduplication, LWW by `updatedAt.toEpochMilli()` then lexicographic `deviceId`, and delete tombstone semantics without I/O, Room, Drive, Android deps, or clock calls. Commits: `b34075c8` (production) and `a4f2fea6` (tests). SPEC moved to `.claude/specs/done/`; next runnable backlog item is `operations-journal-sync-02-journal-table-and-identity-migration`. Verification: `mp-reviewer-android` pass; `mp-tester-android` added `OperationMergerTest`; `mp-runner-android` pass for `:core:domain:test --tests com.kshavrin.mymoney.core.domain.sync.OperationMergerTest`, `:core:domain:test`, `:core:domain:ktlintCheck`, and `:core:domain:compileKotlin`; `mp-verifier-android` pass; `graphify update .` pass. No connected device run was needed for this non-visual domain-only SPEC. No push was performed.
- **2026-06-25 (Claude MP `--feature --next`, shipped + pushed to `main`):** Completed SPEC `dashboard-operations-summary-drawer-03-dashboard-wiring`. Repurposed dashboard taps to the operations summary sheet: Aurora card tap (`onChartClick`→`ChartTapped`) and `BalanceCardClicked` open the **unfiltered** summary; category-tile tap (`SliceClicked`) opens the summary **filtered** to that category (inline accordion removed); data via `GetOperationsSummaryUseCase` over the current `dashboardSelection`; `OperationsSummarySheet` hosted conditionally in `DashboardScreen`; chart settings now reachable **only** via the ⋮ menu (`RightDrawerContent`→`ChartSettingsClicked`); row tap → `TRANSACTION_DETAIL/{id}` and closes the sheet; `NavigateTransactionsBy*` removed. `BalanceCardClicked` handler kept + unit-tested but has no separate UI surface (balance lives inside the Aurora card). Commits: `ab183973` (prod), `efaf4fe0` + `9fc10498` (tests). Pushed `7d2c1528..9fc10498` (also carried the previously-unpushed SPEC 01/02 commits). SPEC → `.claude/specs/done/`; next runnable backlog item is `dashboard-operations-summary-drawer-04-remove-fullscreen-window` (epic NOT yet complete). Verification: `Pixel_5_API_34` (`emulator-5554`) preflight; `mp-reviewer-android` pass; `:feature:dashboard:testDebugUnitTest` 238 green; `:app:compileDebugAndroidTestKotlin` green; connected `DashboardContentUiTest` (summary entry points) + `CategoryTilesListUiTest` green (2 drawer-teardown failures were an `ActivityScenario.close()` cascade flake — pass in isolation); verifier wiring confirmed manually. ⚠ Pre-existing red androidTest `ImportFocusColdStartRegressionTest.importedRowsSurviveColdStartAndShowOnDashboard` confirmed failing at baseline `fc0710a4` too — NOT a SPEC 03 regression; needs a separate fix. Unrelated uncommitted Aurora-wash WIP (Codex session) left untouched in the working tree.
- **2026-06-25 (Codex MP `--feature`, completed locally):** Reverted the rejected Aurora wash expansion attempt and applied the simpler requested adjustment: the dashboard Aurora dark display panel now keeps its original geometry but uses a lighter `DashboardAuroraInnerPanelFill` (`Color.Black` alpha `0.045f`), while the shared compact Aurora balance typography is reduced from `34sp/38sp` to `26sp/30sp` so the balance amount sits inside the dark section with breathing room. Updated token and instrumented typography expectations for the new compact size. Visual artifact: `build\visual-check\mymoney-aurora-lighter-balance-26sp.png`. Verification: `Pixel_5_API_34` preflight (`emulator-5554`, SDK 34, boot complete), `:core:ui:testDebugUnitTest`, `:core:ui:ktlintCheck`, `:feature:dashboard:runKtlintCheckOverMainSourceSet`, `:feature:dashboard:compileDebugKotlin`, `:app:installDebug`, device screenshot pull. `:app:compileDebugAndroidTestKotlin` was retried after a transient Windows file lock and then remained blocked by unrelated stale androidTest sources (`ImportFocusColdStartRegressionTest`, `CategoryTilesListUiTest`) from the operations-summary transition. No commit or push was performed.
- **2026-06-25 (Codex MP `--feature --next`, completed locally):** Completed SPEC `dashboard-operations-summary-drawer-02-summary-sheet-ui` using native MP subagents plus a local test-only resume after subagent quota/capacity limits. Added stateless `OperationsSummarySheet` and `SummaryRecordRow` in `:feature:dashboard`, rendering mixed `SummaryRecord.Operation` / `SummaryRecord.Transfer` rows with localized title/empty/transfer-route strings, a toolbar-preserving sheet height constraint, loading and empty states, row tags, and no `:feature:transactionslist` dependency. Commits: `66154d5f`, `fd8b4d86`, `d97a43d5`, `173bfad9`, `836bf865`. SPEC moved to `.claude/specs/done/`; next runnable backlog item is `dashboard-operations-summary-drawer-03-dashboard-wiring`. Verification: `Pixel_5_API_34` preflight (`emulator-5554`, SDK 34, boot complete), `mp-reviewer-android` first blocked sheet height then passed after repair, `:feature:dashboard:compileDebugKotlin`, `:feature:dashboard:ktlintCheck`, `:app:assembleDebug`, `:app:ktlintCheck`, `:app:compileDebugAndroidTestKotlin`, connected `OperationsSummarySheetUiTest` 5/5 with `0` failed/error/skipped on `Pixel_5_API_34`, and local verifier checklist passed after `mp-verifier-android` was unavailable due model capacity. No push was performed.
- **2026-06-25 (Codex MP `--feature --next`, completed locally):** Completed SPEC `dashboard-operations-summary-drawer-01-summary-usecase` using native MP subagents. Added pure `:core:domain` `SummaryRecord` and `GetOperationsSummaryUseCase`, merging category operations and transfer records into one timestamp-desc summary list, excluding transfers entirely for category-filtered summaries, and deduplicating all-accounts internal transfers by id. Commits: `3f18fbc0`, `efe39d61`. SPEC moved to `.claude/specs/done/`; next runnable backlog item is `dashboard-operations-summary-drawer-02-summary-sheet-ui`. Verification: `mp-reviewer-android` pass, focused `:core:domain:test --tests com.kshavrin.mymoney.core.domain.usecase.GetOperationsSummaryUseCaseTest`, `:core:domain:test`, `:core:domain:ktlintCheck`, `:core:domain:compileKotlin`, `mp-verifier-android` pass, and `graphify update .` pass. No push was performed.
- **2026-06-24 (Codex MP `--feature --next`, completed locally):** Completed SPEC `dashboard-trend-selected-period-02-trailing-trim-stagnation` using native MP subagents. Added pure `:core:domain` `BalanceTrendCalculator.buildAutoSeries(...)`, which caches one snapshot per bucket, trims only the inactive tail after the last active bucket, preserves leading/internal empty buckets as flat stagnation, reindexes the trimmed series, and keeps existing metric semantics for `CUMULATIVE`, `PERIOD_NET`, and `INCOME_EXPENSE`. Commit: `fcf939e3`. SPEC moved to `.claude/specs/done/`; next runnable backlog item is `dashboard-trend-selected-period-03-intraday-2h-buckets`. Verification: `mp-reviewer-android` pass, `:core:domain:test --tests com.kshavrin.mymoney.core.domain.usecase.BalanceTrendCalculatorTest`, `:core:domain:test`, `:core:domain:ktlintCheck`, `:core:domain:compileKotlin`, `mp-verifier-android` pass, and `graphify update .` pass. No push was performed.
- **2026-06-24 (Codex MP `--feature --next`, completed locally):** Completed SPEC `dashboard-trend-selected-period-01-auto-window-per-period` using native MP subagents. Added pure `:core:domain` `BalanceTrendCalculator.buildAutoWindow(...)` for selected-period auto trend buckets: Week -> 7 daily buckets, Month -> one bucket per calendar day including leap February, Year -> 12 monthly buckets, All -> deterministic 30-bucket `CustomRange(earliestDate, today)` with no hidden `now()`, CustomRange <=30 -> daily buckets, CustomRange >30 -> 30 range buckets, and Day rejected for the later intraday path. Commit: `bfb87d68`. SPEC moved to `.claude/specs/done/`; next runnable backlog item is `dashboard-trend-selected-period-02-trailing-trim-stagnation`. Verification: `mp-reviewer-android` pass, `mp-runner-android` pass after one transient Windows file-lock rerun for `:core:domain:test --tests com.kshavrin.mymoney.core.domain.usecase.BalanceTrendCalculatorTest`, plus `:core:domain:test`, `:core:domain:ktlintCheck`, `:core:domain:compileKotlin`, `mp-verifier-android` pass, and `graphify update .` pass. No push was performed.
- **2026-06-23 (Codex MP `--feature --next`, completed locally):** Completed SPEC `category-icon-text-color-01-icon-dominant-color-policy` using native MP subagents. Added pure `:core:common` category color policy helpers: `categoryIconDominantHex(iconKey)` with explicit `#RRGGBB` colors for the current neon category icon key set plus deterministic fallback, `readableTextHex(hex)` with WCAG-style luminance/lightening, and `categoryTextColorHex(iconKey)`. Commits: `8944bc46`, `501282c0`. SPEC moved to `.claude/specs/done/`; epic `category-icon-text-color` remains queued with SPECs 02-04. Verification: `mp-reviewer-android` pass, `:core:common:test --tests com.kshavrin.mymoney.core.common.category.CategoryIconColorTest` pass after one transient Kotlin incremental-cache rerun, `:core:common:test`, `:core:common:compileKotlin`, `:core:common:ktlintCheck`, and `mp-verifier-android` pass. No push was performed pending the user's explicit final approval.
- **2026-06-23 (Codex MP `--feature --next`, completed locally):** Completed SPEC `dashboard-aurora-polish-02-wide-compact-aurora-no-label`. The shared Aurora card now uses an 8dp host side inset instead of the old 245dp max-width cap, applies compact top/bottom/pill/chart spacing, removes the main-dashboard "Balance for ..." label while keeping the top-bar period context, and uses the compact 34sp balance typography for both the main Aurora card and per-currency separate-mode cards. Commits: `87bd1a29`, `ad46ed17`, `cf8f1400`, `e0e85ddd`, `d3fce5b5`, `d569a2c7`; SPEC moved to `.claude/specs/done/` and overview leaves SPEC 03 queued. Verification: `Pixel_5_API_34` preflight (`emulator-5554`, SDK 34, boot complete), `mp-reviewer-android` pass after one token/comment repair, focused app contracts 5/5, `:feature:dashboard` unit tests 157/157, `:app:ktlintCheck`, `:feature:dashboard:ktlintCheck`, `:core:ui:ktlintCheck`, `:app:compileDebugAndroidTestKotlin`, connected `AuroraBalanceCardUiTest` 13/13, connected `CurrencyBalanceCardListUiTest` 20/20, connected `DashboardContentUiTest` 54/54, and `mp-verifier-android` pass. Android backup/data extraction XML also gained explicit sharedpref includes so secure prefs exclusions satisfy lint while remaining excluded. No push was performed pending the user's explicit final approval.
- **2026-06-22 (Codex MP `--feature`, completed locally):** Added 56 new category neon PNG tile assets copied directly from the two supplied 4x7 mockups, preserving the existing 26 reference assets and expanding the shared category picker catalog to 82 distinct bitmap-backed keys in both `:core:designsystem` and `:feature:dictionaries`. New tiles use the current 207x207 rounded-alpha mask, keep transparent outer corners, and are registered through `CategoryIconAssets`; the picker order is old 26, mockup #1 order, then mockup #2 order. Visual artifact: `build\icon-assets-preview\category-neon-contact-sheet-expanded.png`. Verified: `Pixel_5_API_34` preflight (`emulator-5554`, SDK 34, boot complete), PNG count/alpha check (`82` icons, transparent corners), `:core:designsystem:testDebugUnitTest`, `:feature:dictionaries:testDebugUnitTest`, `:core:designsystem:ktlintCheck`, `:feature:dictionaries:ktlintCheck`, `:app:ktlintCheck`, `:app:assembleDebug`, connected `NeonCategoryIconUiTest` 2/2, connected `IconPickerSheetUiTest` + `CategoryEditContentUiTest` 7/7, and `graphify update .` passed. No commit or push was performed.
- **2026-06-22 (Codex MP `--feature`, completed locally):** Reverted the rejected expanded category icon set to the reference-only PNG tile catalog. `:core:designsystem` now registers only the 26 category neon assets from the provided mockup, and both expense/income category pickers expose that same 26-icon set; generated extra keys such as `ic_cat_salary`, `ic_cat_other`, `ic_cat_fastfood`, `ic_cat_hygiene`, and the rest no longer resolve to custom PNG tiles and fall back through the tag tile if encountered in existing data. Removed 41 bad generated `category_neon_*.png` files from `drawable-nodpi` and archived them under `archive/category-neon-extra-icons.rejected-2026-06-22` per project policy. Visual artifact updated at `build\icon-assets-preview\category-neon-contact-sheet.png`. Verified: `Pixel_5_API_34` preflight (`emulator-5554`, SDK 34, boot complete), PNG resource count/alpha check (`26` icons, transparent corners), `:core:designsystem:testDebugUnitTest`, `:feature:dictionaries:testDebugUnitTest`, `:core:designsystem:ktlintCheck`, `:feature:dictionaries:ktlintCheck`, `:app:ktlintCheck`, `:app:assembleDebug`, `NeonCategoryIconUiTest` 2/2, `CategoryEditContentUiTest` 5/5, `IconPickerSheetUiTest` 2/2, and `graphify update .` passed. No commit or push was performed.
- **2026-06-21 (Codex MP `--feature --next`, completed locally):** Completed SPEC `dashboard-balance-trend-chart-04-twenty-visual-styles`. Production commits `dd9deb42` and `511051db` expand `ChartStyle` to the 20 intended public visual styles and dispatch each style inside `BalanceTrendChart` without touching domain/data; test commit `e02e0d84` extends `BalanceTrendChartUiTest` with one-composition-per-test coverage for every style across normal five-point, single-point, all-zero, negative-only, and zero-crossing series. Visual device gate confirmed local `emulator-5554` is `Pixel_5_API_34`, SDK 34, boot complete after NAT attach timeout and local discovery. Verification under JDK 21: `:core:designsystem:ktlintCheck`, `:core:designsystem:compileDebugKotlin`, `:core:designsystem:compileDebugAndroidTestKotlin`, and `:app:assembleDebug` passed; connected `:core:designsystem:connectedDebugAndroidTest` for `com.kshavrin.mymoney.core.designsystem.chart.BalanceTrendChartUiTest` passed `18/18` with zero failures/errors/skips on `Pixel_5_API_34`. Initial final review caught an extra `Line` enum alias and test section comments; both were repaired and `mp-verifier-android` returned `pass:true`. Re-read TDD anchors from repo-local `TDD\MyMoney\MyMoney_TDD.md` lines 181-228, 265-298, 2553-2661, and 2727-2750. SPEC moved to `.claude/specs/done/`; no push was performed.
- **2026-06-21 (Codex build fix):** Fixed the JVM test compile break in `DecisionRouterViewModelTest` after `InitialDataSeeder` gained `CurrencyRateRepository`. Added a local no-rates test stub and passed it into both seeder constructions. Verification under JDK 21 (`C:\Program Files\Microsoft\jdk-21.0.11.10-hotspot`): focused `:app:testDebugUnitTest --tests com.kshavrin.mymoney.navigation.DecisionRouterViewModelTest`, `:app:assembleDebug`, and `graphify update .` passed. The documented Android Studio JBR path was absent on this machine, so the Microsoft JDK 21 install was used. No commit or push was performed.
- **2026-06-20 (Codex MP `--feature`, completed locally):** Implemented the user-provided neon category icon reference across category surfaces. Added reusable `NeonCategoryIcon` / `NeonIconTile` styling in `:core:designsystem`, neon accent mapping for every known category key with stable fallback accents for future/missing keys, and custom stroke-vector replacements for the reference-like categories (food, bills, entertainment, taxi, housing, sport, gifts, phone, transport, cafe, car, plus related mappings). Applied the new rounded dark neon tiles in category pickers, transaction form category grid, dashboard category tiles, dictionaries category list/edit, transactions-list category headers, and the settings import wizard icon picker. Accessibility/localization cleanup: picker cells keep internal keys as `testTag`s while TalkBack gets localized generic icon-option labels; category/account edit buttons no longer show raw `state.iconKey`. Tests added/updated: `CategoryIconsTest`, `NeonCategoryIconTest`, pixel-based `NeonCategoryIconUiTest`, `IconPickerSheetUiTest`, and account picker coverage. Visual artifact: `build\visual-check\mymoney-category-grid-neon-final.png`. Verification under JDK 21: `:core:designsystem:testDebugUnitTest`, `:core:designsystem:compileDebugAndroidTestKotlin`, `:app:compileDebugAndroidTestKotlin`, `:app:ktlintCheck`, `:core:designsystem:ktlintCheck`, `:feature:dictionaries:ktlintCheck`, `:feature:dashboard:ktlintCheck`, `:feature:transactionslist:ktlintCheck`, `:feature:settings:ktlintCheck`, and `:app:assembleDebug` passed. Device gate on `Pixel_5_API_34`: `NeonCategoryIconUiTest` 2/2, `IconPickerSheetUiTest` 2/2, and the focused `AccountEditContentUiTest#account icon picker opens and emits selected icon key` 1/1 passed. The full `AccountEditContentUiTest` class had an earlier transient Compose-host "No compose hierarchies found" run, but the changed picker scenario was rerun and passed. While resuming, an unrelated new `RateConfirmDialog`/`RateConfirmDialogUiTest` pair in the working tree blocked compile/ktlint; missing EN/RU rate strings and ktlint-only formatting/import fixes were applied so the tree remains buildable. `graphify update .` passed after the final edits. The final `mp-reviewer-android` retry was unavailable due an account usage limit, so the equivalent local review checked TDD anchors, feature-to-core dependency boundaries, localization, accessibility, and test coverage. No commit or push was performed.
- **2026-06-19 (Claude MP `--feature`, completed + pushed):** Reframed the dashboard neon ring as a financial-health gauge (standalone feature, not a backlog SPEC). New fill/color semantics replace the old `expense/income` green fill: GREEN (mint→cyan) when `income>0 && expense<=income`, fraction `net/income`; RED (new coral→red neon gradient + glow) when overspent or income-less — `(expense-income)/income` if income present, else `currentExpense/previousPeriodExpense` (prev==0 → full red); empty track when no records. Commits: `e7a156b6` red ring gradient tokens (`neonRingGradientStartExpense/EndExpense` in core/ui Color.kt); `fac2f766` pure `RingGaugeCalculator` (:core:domain), `ringIsExpense` state field, additive `gradientStart/gradientEnd` params on `NeonRingChart` (default green, backward-compatible incl. glow), DashboardViewModel computes the gauge + lazily fetches the previous-period snapshot ONLY when `income==0 && expense>0` via `period.previous()`, DashboardScreen selects red/green tokens by mode; `72d48210` fix — moved `snapshotToSlices/ExpenseTiles` after recompute suspension points so the new previous-period fetch no longer clobbers budget-alert slice flags (latent race exposed by the extra suspension); `1f9b5544` tests (RingGaugeCalculatorTest all branches, DashboardViewModelTest reconciled stale fixtures 0.557→0.4429 green + 0f→1.0 red + 5 new branch tests, NeonRingChartUiTest +5 gradient-param tests). Verification under JDK 21: `:core:domain:test` RingGaugeCalculatorTest green, `:feature:dashboard:testDebugUnitTest` DashboardViewModelTest 51/51 green, ktlint (core:domain/core:ui/core:designsystem/feature:dashboard) green after `:core:domain:ktlintFormat`, `:app:assembleDebug` + androidTest compile ok. Device gate on `emulator-5554` (Pixel_5_API_34, SDK 34): `NeonRingChartUiTest` 7/7, `DashboardContentUiTest` 31/31 (one prior run hit a flaky `INSTRUMENTATION_ABORTED` emulator crash on the long swipe test; clean re-run 31/31 confirmed environmental). Reviewer `{"pass":true}` ×2, Verifier `pass:true`. Open decision baked in (user-chosen): income-present overspend → red `(expense-income)/income`. Pushed to main.
- **2026-06-19 (Codex MP `--feature --next`, completed):** Completed SPEC `dashboard-neon-fidelity-04-fabs-smaller-no-labels` and closed the four-SPEC `dashboard-neon-fidelity` epic. Production commit `24b3aaa0` shrinks both dashboard FABs from 104dp to 94dp, proportionally reduces their outline and icon, removes the visible EXPENSE/INCOME labels, and preserves localized Expense/Income accessibility descriptions and click behavior. Test and repair commits `debac840`, `4d0315af`, and `8c70b447` update dashboard/journey Compose coverage, pin the 94dp/29dp/3.6dp tokens, and repair pre-existing ktlint failures exposed by the app-wide gate. Visual gate confirmed local `emulator-5554` is `Pixel_5_API_34`, SDK 34, boot complete after NAT attach timeout and local discovery. Production and final `mp-reviewer-android` runs returned `{"pass":true,"violations":[]}`; final `mp-runner-android` passed focused `DashboardBalancePanelColorsTest`, `:core:ui:ktlintCheck`, `:feature:dashboard:ktlintCheck`, `:app:ktlintCheck`, `:app:compileDebugAndroidTestKotlin`, and `:app:assembleDebug`; `mp-runner-instrumented-android` passed `DashboardContentUiTest` 31/31 with zero failed/error/skipped on device. The final `mp-verifier-android` subagent was unavailable because its account usage limit was reached, so the equivalent read-only wiring checklist was completed locally: no feature-to-feature dependency, EN/RU resource parity retained, no hardcoded FAB copy, accessibility descriptions/click paths covered, and no stale 104dp assertion. Re-read TDD anchors from repo-local `TDD\MyMoney\MyMoney_TDD.md` lines 181-228, 265-298, 2553-2661, and 2727-2750. SPEC 04 and the epic overview moved to `.claude/specs/done/`; no runnable backlog SPEC remains and no push was performed pending the user's explicit final approval.
- **2026-06-19 (Codex MP `--feature --next`, completed):** Completed SPEC `dashboard-neon-fidelity-03-center-plate-fill-divider`. Production commit `412b9591` enlarges the dashboard ring-center income/expense badge, adds divider/color/spacing/typography tokens, stretches the badge to the lower inner-ring zone with bounded insets and max height, and keeps the center read-only/presentation-only. Test commits `a8e18255` and `d611d342` add the allowed badge/divider test tags, pure `RingCenterLayoutTest` coverage for divider-aware badge height/insets/fitting, and connected `RingCenterContentUiTest` coverage for the 56dp badge, 1dp divider, 32dp side insets, 16dp bottom inset, and no-click behavior; the second test commit corrected an over-strict lower-zone assertion found by the first runner pass. Visual gate confirmed local `emulator-5554` is `Pixel_5_API_34`, SDK 34, boot complete after NAT attach timeout and local discovery. Subagent verification passed: production, full, and repair `mp-reviewer-android` runs returned `{"pass":true,"violations":[]}`; final `mp-runner-android` passed focused `RingCenterLayoutTest`, `:feature:dashboard:ktlintCheck`, `:core:ui:ktlintCheck`, `:app:compileDebugAndroidTestKotlin`, and `:app:assembleDebug`; `mp-runner-instrumented-android` passed `RingCenterContentUiTest` 6/6 with zero failed/error/skipped on device; final `mp-verifier-android` passed static checks. Re-read TDD anchors from repo-local `TDD\MyMoney\MyMoney_TDD.md` lines 181-228, 265-298, 2553-2661, and 2727-2750. SPEC moved to `.claude/specs/done/`; no push was performed pending the user's explicit final approval. Next runnable backlog item: `dashboard-neon-fidelity-04-fabs-smaller-no-labels.md`.
- **2026-06-19 (Codex MP `--feature --next`, completed):** Completed SPEC `dashboard-neon-fidelity-02-ring-smaller-raised`. Production commits `69db2038` and `b8f5a159` shrink the neon dashboard ring tokens from 248dp/20dp to 200dp/16dp while preserving the stroke-aware glow container margin so the blur is not clipped; final net production behavior keeps `NeonRingChart` non-clickable and presentation-only. Test commit `04af3f94` updates pure layout and connected Compose coverage for the 200dp ring contract, 264dp glow container, 160dp inner slot, and dashboard ring bounds/non-clickability. Visual gate confirmed local `emulator-5554` is `Pixel_5_API_34`, SDK 34, boot complete after NAT attach failure and local discovery. Subagent verification passed: first `mp-reviewer-android` blocked an undercounted glow margin, then final production and full reviewers returned `{"pass":true,"violations":[]}`; `mp-runner-android` passed focused `NeonRingChartTest`, `:core:ui:ktlintCheck`, `:core:designsystem:ktlintCheck`, `:app:ktlintCheck`, `:core:designsystem:compileDebugAndroidTestKotlin`, `:app:compileDebugAndroidTestKotlin`, and `:app:assembleDebug`; `mp-runner-instrumented-android` passed `DashboardContentUiTest` 31/31 with zero failed/skipped on device, and the corrected core-module XML for `NeonRingChartUiTest` shows 2/2 with zero failed/error/skipped after an earlier app-task mis-target produced a class-loading failure; final `mp-verifier-android` passed static checks. Re-read TDD anchors from repo-local `TDD\MyMoney\MyMoney_TDD.md` lines 181-228, 265-298, 2553-2661, and 2727-2750. SPEC moved to `.claude/specs/done/`; no push was performed pending the user's explicit final approval. Next runnable backlog item: `dashboard-neon-fidelity-03-center-plate-fill-divider.md`.
- **2026-06-19 (Codex MP `--feature --next`, completed):** Completed SPEC `dashboard-neon-fidelity-01-thin-toolbar-period-merge`. Production commit `25748bd5` replaces the S01 hero app bar plus separate 80dp period row with a compact one-row neon toolbar: menu/back + transfer on the left, current period switcher in the center, search + overflow on the right, with MyMoney wordmark and currency subtitle removed and drawer/search/transfer/more/period/swipe behavior preserved. Test commits `653678bb` and `95aab200` update dashboard and launch instrumentation coverage for the new toolbar signal and fix Compose test API compatibility. Visual gate confirmed local `emulator-5554` is `Pixel_5_API_34`, SDK 34, boot complete after NAT attach timeout and local discovery. Subagent verification passed: final `mp-reviewer-android` returned `{"pass":true,"violations":[]}`; `mp-runner-android` passed `:feature:dashboard:ktlintCheck`, `:app:compileDebugAndroidTestKotlin`, and `:app:assembleDebug`; `mp-runner-instrumented-android` passed `DashboardContentUiTest` 31/31 and `MainActivityLaunchTest` 1/1 with zero failed/error/skipped on device; final `mp-verifier-android` passed static checks. Re-read TDD anchors from repo-local `TDD\MyMoney\MyMoney_TDD.md` lines 181-228, 265-298, 2553-2661, and 2727-2750. SPEC moved to `.claude/specs/done/`; no push was performed pending the user's explicit final approval. Next runnable backlog item: `dashboard-neon-fidelity-02-ring-smaller-raised.md`.
- **2026-06-18 (Codex MP `--feature --next`, completed):** Completed SPEC `dashboard-neon-ring-redesign-06-dashboard-assembly` and closed the six-SPEC `dashboard-neon-ring-redesign` epic. Integrated `NeonRingChart`, non-clickable `RingCenterContent`, scrollable expense `CategoryTilesList`, neon top bar, and 104dp expense/income FABs into `DashboardContent`; category tiles preserve AS-3 filtered transaction navigation while ring/pill taps do nothing. Production/test commits: `b930f36c`, `9cdea858`, `74b23fe4`, `c171caa8`, `f5f1ade9`; inherited verified SPEC 05 files were committed as `401b7710`. Visual gate confirmed local `emulator-5554` is `Pixel_5_API_34`, SDK 34, boot complete. Subagent verification passed after one fixture correction and one verifier-found locked-decision fix: final reviewers returned `{"pass":true,"violations":[]}`; `mp-runner-android` passed 51 tests plus dashboard/app ktlint, production/androidTest compile, dashboard assembly, and `:app:assembleDebug`; the fresh post-fix connected `DashboardContentUiTest` run passed 31/31 with zero failed/skipped; final `mp-verifier-android` passed all static checks. Re-read TDD anchors lines 181-228, 265-298, 2553-2661, and 2727-2750. SPECs 01-06 and the epic overview moved to `.claude/specs/done/`. Changes are committed locally but not pushed pending the user's explicit final approval.
- **2026-06-18 (Codex MP `--feature --next`, completed):** Completed SPEC `dashboard-neon-ring-redesign-05-category-tiles-list`. Added `CategoryTile` and `CategoryTilesList` for the neon dashboard category breakdown: amount-bearing tile state, parsed category color/icon chip, integer `MoneyFormatter` amounts, fraction-width progress bar, tile tap callback, and localized empty-period copy. Extended `DashboardViewModelTest` for tile amount/icon/color propagation and added connected Compose UI coverage in `CategoryTilesListUiTest`. Visual gate confirmed local `emulator-5554` is `Pixel_5_API_34`, SDK 34, boot complete. Subagent verification passed: production and final `mp-reviewer-android` returned `{"pass":true,"violations":[]}`; `mp-runner-android` passed focused `DashboardViewModelTest`, `:feature:dashboard:ktlintCheck`, `:app:compileDebugAndroidTestKotlin`, and `:app:assembleDebug`; `mp-runner-instrumented-android` passed `CategoryTilesListUiTest` 2/2 with zero failed/error/skipped on device; `mp-verifier-android` passed static checks. Re-read TDD anchors from repo-local `TDD\MyMoney\MyMoney_TDD.md` lines 181-228, 265-298, 2553-2661, and 2727-2750. `graphify update .` passed. SPEC moved to `.claude/specs/done/`; changes remain local and uncommitted/unpushed. Next runnable backlog item: `dashboard-neon-ring-redesign-06-dashboard-assembly.md`.
- **2026-06-18 (Codex tooling):** Improved the project-local MP Dev device gate so a failed or hanging `adb connect 10.0.2.2:5555` no longer causes an immediate stop. Updated `AGENTS.md`, the `mp-dev` Codex skill, the instrumented-runner extras/shim, and the device verification runbooks to try local `adb devices -l` discovery first and accept any serial that reports `Pixel_5_API_34`, SDK 34, and boot-complete before asking the user for a new connection.
- **2026-06-18 (Codex MP `--feature --next`, completed):** Completed SPEC `dashboard-neon-ring-redesign-04-ring-center-balance`. Production commits `dc5c9902`, `c6a137e5`, and `eb73f9a0` add reusable `RingCenterContent`: localized Balance/Остаток label, display-only integer `MoneyFormatter` amounts, a vertically stacked green income/coral expense pill, and deterministic width/height fitting with readable typography floors for the complete center content. Test commits `981dc994`, `9aba503c`, and `5e7a1d04` add connected Compose UI coverage plus pure JVM layout-contract coverage; the last commit repairs two invalid Compose test imports found by the first device compile. Visual gate confirmed local `emulator-5554` is `Pixel_5_API_34`, SDK 34, boot complete. Subagent verification passed: final `mp-reviewer-android` returned `{"pass":true,"violations":[]}`; `mp-runner-android` passed focused `RingCenterLayoutTest` (5 tests), `:feature:dashboard:ktlintCheck`, production/androidTest compile and assembly, and `:app:assembleDebug`; `mp-runner-instrumented-android` passed `RingCenterContentUiTest` 5/5 with zero failed/skipped on fresh device XML; `mp-verifier-android` passed all static checks. Re-read TDD anchors from repo-local `TDD\MyMoney\MyMoney_TDD.md` lines 181-228, 265-298, and 2553-2661. `graphify update .` passed. SPEC moved to `.claude/specs/done/`; no push was performed. Next runnable backlog item: `dashboard-neon-ring-redesign-05-category-tiles-list.md`.
- **2026-06-18 (Codex MP `--feature --next`, completed):** Completed SPEC `dashboard-neon-ring-redesign-03-neon-ring-chart`. Production commits `ffe9b6c3` and `dc2cd5d7` add the reusable `NeonRingChart` design-system component: a rounded mint-to-cyan gradient arc over the dark neon track, proportional sweep from the supplied fraction, a zero-fraction track-only state, a centered inner content slot, and conservative outer space for an unclipped blurred glow without changing `MonefyDonutChart`. Test/fix commit `877e5666` adds pure layout-contract coverage plus connected Compose UI bounds/slot coverage. Visual gate confirmed `emulator-5554` is `Pixel_5_API_34`, SDK 34, boot complete before work and again before device execution. Subagent verification passed after one narrow androidTest import repair: final `mp-reviewer-android` returned `{"pass":true,"violations":[]}`; `mp-runner-android` passed focused `NeonRingChartTest`, `:core:designsystem:ktlintCheck`, production/androidTest compile and assembly, and `:app:assembleDebug`; `mp-runner-instrumented-android` passed `NeonRingChartUiTest` 2/2 with zero failed/skipped on device; `mp-verifier-android` passed all static checks. Re-read TDD anchors from repo-local `TDD\MyMoney\MyMoney_TDD.md` lines 181-228, 265-298, 2553-2661, and 2727-2750. Graphify's post-commit hook rebuilt the graph with the new component. SPEC moved to `.claude/specs/done/`; no push was performed. Next runnable backlog item: `dashboard-neon-ring-redesign-04-ring-center-balance.md`.
- **2026-06-18 (Codex MP `--feature --next`, completed):** Completed SPEC `dashboard-neon-ring-redesign-02-dashboard-state-neon`. Production commit `12eb605f` adds neon dashboard state fields `periodNet`, `ringFraction`, and `expenseTiles`, deriving them from the existing `BalanceSnapshot` without new repository calls while preserving old `snapshotToSlices()` OTHER grouping for the current donut. Test/fix commit `7eac8fbd` filters reserved synthetic `OTHER_CATEGORY_ID` out of `expenseTiles` and extends `DashboardViewModelTest` for the 85000/47350 ring fraction + net fixture, zero-income negative-net fixture, all-real expense tiles sorted descending with sub-2% categories, non-expense exclusion, and budget-alert propagation. Visual gate confirmed `emulator-5554` is `Pixel_5_API_34`, SDK 34, boot complete before work. Subagent verification passed: `mp-reviewer-android` twice returned `{"pass":true,"violations":[]}`; `mp-runner-android` passed focused `DashboardViewModelTest`, `:feature:dashboard:ktlintCheck`, `:feature:dashboard:compileDebugKotlin`, and `:app:assembleDebug`; `mp-verifier-android` passed all static checks and noted no connected UI test was required for this presentation-state/unit slice. Re-read TDD anchors from repo-local `TDD\MyMoney\MyMoney_TDD.md` lines 181-228, 265-298, 1172-1207, and 2553-2661. SPEC moved to `.claude/specs/done/`; no push was performed. Next runnable backlog item: `dashboard-neon-ring-redesign-03-neon-ring-chart.md`.
- **2026-06-18 (Codex MP `--feature --next`, completed):** Completed SPEC `dashboard-neon-ring-redesign-01-neon-design-tokens`. Commits `7b6f789c`, `15c518a3`, and `674622e5` add the neon app palette/dashboard ring-tile-FAB tokens, collapse `MyMoneyTheme(darkTheme=...)` to the neon palette, keep Material `onSurfaceVariant` readable with companion `#8E96A8` against `surfaceVariant #1B2236` while preserving explicit dashboard `textSecondary #7C8290`, set `dashboardFabSize=104dp`, and remove negative letter spacing from the new ring balance typography. `DashboardBalancePanelColorsTest` covers ThemeMode palette collapse, ring/tile/accent tokens, FAB size, and contrast. Visual gate confirmed `emulator-5554` is `Pixel_5_API_34`, SDK 34, boot complete. Subagent verification passed: `mp-reviewer-android` twice returned `{"pass":true,"violations":[]}`; `mp-runner-android` passed focused `DashboardBalancePanelColorsTest`, `:core:ui:ktlintCheck`, and `:core:ui:compileDebugKotlin`; `mp-verifier-android` passed all static checks. Device smoke artifact remains at `build\visual-check\mymoney-neon-token-check-2.png`. SPEC moved to `.claude/specs/done/`; no push was performed. Next runnable backlog item: `dashboard-neon-ring-redesign-02-dashboard-state-neon.md`.
- **2026-06-18 (Codex MP `--feature --next`, completed):** Completed SPEC `money-decimal-precision-05-migration-round-stored-money` and closed the money-decimal-precision epic. Production commit `af59efc1` adds mandatory `MIGRATION_4_5`, rounds the eight stored monetary REAL columns with SQLite `ROUND(col, 2)` plus NULL guards, preserves percentage/term fields, bumps Room schema to 5, exports schema `5.json`, and registers the migration. Test commits `70f4da8b` and `c673669f` add focused `MigrationTestHelper` coverage for all monetary columns, NULL values, and excluded `annual_rate_percent`, `term_months`, and `exchange_rate` fields. Production and final reviews passed; connected `MoneyDatabaseMigration4To5Test` passed on `Pixel_5_API_34` with `tests=3 failures=0 errors=0 skipped=0`; `:core:database:ktlintCheck`, `:core:database:kspDebugKotlin`, and `:app:assembleDebug` passed under `C:\Program Files\Microsoft\jdk-21.0.11.10-hotspot`. Re-read TDD anchors from repo-local `TDD\MyMoney\MyMoney_TDD.md` lines 181-228, 1172-1207, and 2553-2661. Manual restore/upgrade smoke remains on the verifier checklist; no push was performed. Next runnable backlog item: `dashboard-neon-ring-redesign-01-neon-design-tokens.md`.
- **2026-06-16 (Codex MP `--feature --next`, completed):** Completed SPEC `money-decimal-precision-04-import-amount-rounding`. Commit `578abe5f` normalizes imported CSV money amounts at the repository write boundary in `BackupRepositoryImpl` using the existing `BigDecimal.toMoneyScale(currency)` policy. MyMoney CSV import now rounds `amount` before upsert and rounds transfer `toAmount` with the target account currency; Monefy CSV import resolves the row currency and rounds before upsert. AppendDedup keys for both import paths also use the rounded money scale so re-importing long-tail values does not bypass dedup after the first rounded insert. `mp-developer-android` implemented the production diff; `mp-reviewer-android` first caught raw-amount AppendDedup keys, then passed after the fix; `mp-tester-android` added instrumented Room/import coverage; `mp-verifier-android` passed. Verification passed under `C:\Program Files\Microsoft\jdk-21.0.11.10-hotspot`: `:core:database:ktlintFormat`, `:core:database:ktlintCheck`, connected `:core:database:connectedDebugAndroidTest` for `BackupCsvTransferTest` through `scripts/run_connected_test_on_host_avd.ps1` with parsed XML `tests=7 failures=0 errors=0 skipped=0`, connected `MonefyCsvImportE2ETest` through the helper on `Pixel_5_API_34` with parsed XML `tests=23 failures=0 errors=0 skipped=0`, and `:app:assembleDebug`. Device preflight confirmed `emulator-5554` is `Pixel_5_API_34`, SDK 34, boot complete. Re-read TDD anchors from repo-local `TDD\MyMoney\MyMoney_TDD.md` lines 181-228, 1172-1207, and 2553-2661. `graphify update .` passed. SPEC moved to `.claude/specs/done/`. Next runnable backlog item: `money-decimal-precision-05-migration-round-stored-money.md`.
- **2026-06-16 (Codex MP `--feature --next`, completed):** Completed SPEC `money-decimal-precision-03-form-money-display-and-input-scale`. Production commits `34538f06` and `ec2fddb3` route Goal edit balance/remaining/projection money strings through `MoneyFormatter`, carry the selected account `Currency` in `GoalEditState`, apply `toMoneyScale(currency)` to Goal and Account money field parsing, scale advanced contribution row-derived monthly values, and round Currency Rate input to the SPEC's 2-decimal assumption. Test commit `a80e87af` adds focused JVM coverage for Goal balance/capital-delta formatting without FP tails, Goal save rounding including advanced contribution rows, Account initial-balance rounding, and Currency Rate comma/extra-decimal rounding. Hygiene commit `a0bc75da` formats blocking ktlint violations in the touched module test source sets, including pre-existing `AccountsListViewModelTest` and `TransferViewModelTest` lines required for module-level ktlint gates. `mp-reviewer-android` passed production diffs after one follow-up; final test-review/verifier subagents were blocked by the account usage limit, so final review/verifier checks were completed locally. Verification passed under `C:\Program Files\Microsoft\jdk-21.0.11.10-hotspot`: `:feature:dictionaries:testDebugUnitTest --tests com.kshavrin.mymoney.feature.dictionaries.accounts.AccountEditViewModelTest --tests com.kshavrin.mymoney.feature.dictionaries.goals.GoalEditViewModelTest --tests com.kshavrin.mymoney.feature.dictionaries.goals.GoalEditSavingsViewModelTest`, `:feature:transaction:testDebugUnitTest --tests com.kshavrin.mymoney.feature.transaction.rate.CurrencyRateViewModelTest`, `:feature:dictionaries:ktlintCheck`, `:feature:transaction:ktlintCheck`, and `:app:assembleDebug`. Non-visual presentation/JVM work, so no connected-device gate was required. Re-read TDD anchors from repo-local `TDD\MyMoney\MyMoney_TDD.md` lines 181-228, 1172-1207, and 2553-2661. SPEC moved to `.claude/specs/done/`. Next runnable backlog item: `money-decimal-precision-04-import-amount-rounding.md`.
- **2026-06-15 (Codex MP `--feature --next`, completed):** Completed SPEC `money-decimal-precision-02-balance-calculator-rounding`. Production commit `be8df9b8` routes `BalanceCalculator` category subtotals plus aggregate income, expense, net, and combined totals through `BigDecimal.toMoneyScale(currency)` at the domain boundary, removing SQL Double FP tails such as `-1182337.0799999996` from emitted `BalanceSnapshot`/`CategoryBalance` money values. Test commit `6e101cae` extends `BalanceCalculatorTest` for the SPEC fixtures (`0.30000000000000004 -> 0.30`, `1182337.0799999996 -> 1182337.08` expense / `-1182337.08` net, empty summaries -> `0.00`) plus a JPY scale-0 regression proving the account currency policy is used. `mp-reviewer-android` passed production and final test diffs (0 violations); `mp-runner-android` passed `:core:domain:test --tests com.kshavrin.mymoney.core.domain.usecase.BalanceCalculatorTest --tests com.kshavrin.mymoney.core.domain.model.MoneyScaleTest` and `:core:domain:ktlintCheck` under `C:\Program Files\Microsoft\jdk-21.0.11.10-hotspot`; an earlier concurrent ktlint/test attempt failed only on KSP cache flushing and passed on sequential rerun. `mp-verifier-android` passed with static checks for scope, boundary rounding, normalized snapshot totals, SPEC fixture coverage, build/lint verification, and no device requirement. Non-visual domain/JVM work, so no connected-device gate was required. Re-read TDD anchors lines 181-228, 1172-1207, and 2553-2661. SPEC moved to `.claude/specs/done/`. Next runnable backlog item: `money-decimal-precision-03-form-money-display-and-input-scale.md`.
- **2026-06-15 (Codex MP `--feature --next`, completed):** Completed SPEC `money-decimal-precision-01-domain-money-scale-policy`. Production commit `e98744a4` adds canonical `BigDecimal.toMoneyScale(currency)` in `:core:domain` with `scale = min(currency.decimalDigits, 2)` and `RoundingMode.HALF_UP`, then routes `Money.plus`, `Money.minus`, `Money.times`, and `Money.zero` through it. Test commit `d5b75d85` adds `MoneyScaleTest` for the SPEC fixtures (`-1182337.0799999996 -> -1182337.08`, `10.005 -> 10.01`, 3-decimal currency cap, JPY whole-unit rounding, zero scale preservation) and updates `MoneyTest` stale arithmetic/zero coverage for a 3-decimal currency. `mp-reviewer-android` passed production and test diffs (0 violations); `mp-runner-android` passed `:core:domain:test --tests com.kshavrin.mymoney.core.domain.model.MoneyScaleTest --tests com.kshavrin.mymoney.core.domain.model.MoneyTest` under JDK 21; `mp-verifier-android` passed with no manual checklist. Non-visual domain work, so no connected-device gate was required. Re-read TDD anchors lines 181-228, 1172-1207, and 2553-2661. SPEC moved to `.claude/specs/done/`. Next runnable backlog item: `money-decimal-precision-02-balance-calculator-rounding.md`.
- **2026-06-15 (Codex tooling):** Updated project-local MP tooling references to `mobile-pipeline` `1.8.1`: `.agents/skills/mp-dev/SKILL.md`, `AGENTS.md`, MP Dev Codex shims, and MP Spec Codex shims now point at the installed `mp-dev`/`mp-spec` `1.8.1` plugin cache. No app code or phase state changed.
- **2026-06-13 (Claude MP `/mp --feature --next`, completed):** Completed SPEC `audit8-hygiene-01-onboarding-flag-skip`. Production commit `e1346bab` moves the `SHOW_ONBOARDING` `buildConfigField` from `defaultConfig` into `buildTypes` (debug=false, release=true), adds `OnboardingFlagModule` (Hilt) feeding the flag into `DecisionRouterViewModel`, and fixes the skip path so that when the flag is false and `onboardingCompletedAt == null` the router stamps `onboardingCompletedAt` and routes to Dashboard (Splash loop stops, app-shortcuts in the Dashboard branch work) while `seedIfNeeded` still runs. Test commit `3a19046f` adds `DecisionRouterViewModelTest` (7 fakes-only tests: flag×completedAt routing across Splash/Dashboard branches + stamp + seed invariants + Pending resolution). `mp-reviewer-android` passed (0 violations); the `mp-runner-android` script gave its known `:app:` detekt/jacoco false-negative, so verification was done directly: `:app:testDebugUnitTest --tests DecisionRouterViewModelTest` BUILD SUCCESSFUL and `:app:ktlintCheck` green (test required `:app:ktlintFormat` first — tester emitted unformatted multiline-expression/blank-line/function-signature violations). `mp-verifier-android` passed (nav_wired ok, hilt_graph ok, tests_exist ok, stale_tests ok). Non-visual unit work, so no connected-device gate. `mp-docs` is inert here. SPEC moved to `.claude/specs/done/`. Next runnable backlog item: `audit8-hygiene-02-dead-code-unlink.md`.
- **2026-06-13 (Codex MP feature --next, completed):** Completed active SPEC `audit7-forms-hardening-04-domain-misc-fixes`. Production commits `03943e6e` and `a7b9573b` make initial seeding atomic through a domain `TransactionRunner` backed by Room `withTransaction`, keep seeder idempotence by the currencies guard, report recoverable Splash seeder failures to Sentry, rethrow coroutine cancellation, expose a localized Splash retry affordance, set `MoneyFormatter` rounding to `HALF_UP`, and make calculator `100 ÷ 0 =` preserve the left operand while clearing the pending operation. Test commit `2f5e19a5` updates `InitialDataSeederTest`, `SplashViewModelTest`, `MoneyFormatterTest`, and `CalculatorEngineTest` for rollback/retry/HALF_UP/divide-by-zero coverage. `mp-developer-android` returned the existing production commit; `mp-reviewer-android` initially blocked swallowed `CancellationException`; `mp-tester-android` authored the focused unit tests; final reviewer/verifier spawning was blocked by the subagent thread limit, so the cancellation/retry follow-up and final review were completed locally against the MP reviewer checklist. Verification passed with `C:\Program Files\Microsoft\jdk-21.0.11.10-hotspot`: `:core:domain:test --tests com.kshavrin.mymoney.core.domain.seed.InitialDataSeederTest`, `:core:common:test --tests com.kshavrin.mymoney.core.common.money.MoneyFormatterTest --tests com.kshavrin.mymoney.core.common.calculator.CalculatorEngineTest`, `:feature:onboarding:testDebugUnitTest --tests com.kshavrin.mymoney.feature.onboarding.SplashViewModelTest`, and `:app:assembleDebug`. This was unit/domain work, so no connected-device gate was required. Re-read available TDD anchors from repo-local `TDD\MyMoney\MyMoney_TDD.md`: seed contents lines 1956-1961, calculator/business rules lines 1172-1207, and testing strategy lines 2553-2661. SPEC moved to `.claude/specs/done/`. Next runnable backlog item: `audit8-hygiene-01-onboarding-flag-skip.md`.
- **2026-06-13 (Codex MP feature --next, completed):** Completed SPEC `audit6-quality-gates-04-vm-tests-core-flows`. Test commit `9f243319` adds the first `:feature:onboarding` JVM tests with module-local fakes and test dependencies: `OnboardingViewModelTest` covers onboarding completion persistence/idempotence, and `SplashViewModelTest` covers one-time seed routing plus current seeder-failure behavior. It also extends `TransferViewModelTest` for zero-amount validation, AS-6 missing-rate navigation, and AS-7 cross-currency stored-rate save with `NavigateBack`. `mp-tester-android` authored the tests; `mp-reviewer-android` passed before and after a narrow Splash failure-test repair; `mp-runner-android` first caught the bad `assertThrows` placement, then passed fresh `--rerun-tasks` runs for `:feature:onboarding:testDebugUnitTest --tests com.kshavrin.mymoney.feature.onboarding.OnboardingViewModelTest --tests com.kshavrin.mymoney.feature.onboarding.SplashViewModelTest` and `:feature:transaction:testDebugUnitTest --tests com.kshavrin.mymoney.feature.transaction.transfer.TransferViewModelTest`; `:app:assembleDebug` passed earlier in the runner pass. `mp-verifier-android` passed with a manual checklist covering onboarding completion, splash seed/failure behavior, zero-amount transfer validation, missing-rate navigation, and stored-rate cross-currency save. This was non-visual JVM test work, so no connected-device gate was required. The recorded TDD path `C:\Pet\MyMoney\TDD\MyMoney\MyMoney_TDD.md` was unavailable in this environment, so no TDD line-anchor re-read was claimed. `graphify update .` was launched by the test agent and the post-commit hook launched a background rebuild. SPEC moved to `.claude/specs/done/`. Next backlog item: `audit6-quality-gates-05-vm-tests-dictionaries.md`.
- **2026-06-12 (Codex MP feature --next, completed):** Completed SPEC `audit3-lock-security-04-backup-rules-recovery`. Production commit `bb487398` replaces template Auto Backup/Data Extraction XML with explicit database/DataStore includes and unconditional secure-prefs exclusion, adds `SecureStorageImpl` recovery for `GeneralSecurityException`/`IOException` with Sentry reporting and prefs deletion/recreation, resets DataStore lock enablement when recovered prefs have no PIN hash, and lets `LockController` immediately hide an already-shown lock overlay when lock is disabled. Test commit `281e56ed` adds XML rule tests, JVM SecureStorage recovery/lock-reset coverage, connected `SecureStorageTest` recovery cases, and LockController disable-after-overlay coverage. `mp-reviewer-android` initially blocked swallowed exceptions, then a brace/constructor regression, and passed after fixes; test reviewer passed after switching connected tests back to `runTest` with an androidTest coroutines-test dependency. `mp-runner-android` passed focused `BackupRulesTest`, `DataExtractionRulesTest`, `SecureStorageImplTest`, `LockControllerTest`, and `:app:assembleDebug` using `C:\Program Files\Microsoft\jdk-21.0.11.10-hotspot` after two invalid-JDK attempts. Device gate confirmed `emulator-5554` is `Pixel_5_API_34` (SDK 34, boot complete); an initial app-APK connected run failed with `ClassNotFoundException`, then `mp-runner-instrumented-android` passed `:core:datastore:connectedDebugAndroidTest --tests com.kshavrin.mymoney.core.datastore.SecureStorageTest` 9/9 on `Pixel_5_API_34`. `mp-verifier-android` passed with restore/corrupt-secure-prefs manual checklist; `mp-docs` inert. The recorded TDD path `C:\Pet\MyMoney\TDD\MyMoney\MyMoney_TDD.md` was unavailable in this environment, so no TDD line-anchor re-read was claimed. `graphify update .` was launched by the post-commit hook. SPEC moved to `.claude/specs/done/`. Next backlog item: `audit4-records-01-transfers-tab.md`.
- **2026-06-12 (Codex MP feature --next, completed):** Completed SPEC `audit3-lock-security-03-flag-secure-cold-start`. Production commit `db4e45d8` adds lock-state resolution to `LockController`, holds the Android splash screen until the first settings emission, and toggles `WindowManager.LayoutParams.FLAG_SECURE` from `MainActivity` while preserving the AS-5 lock overlay model. Test commit `7591b502` adds JVM `LockController.isResolved` coverage plus connected `MainActivityWindowSecurityTest` assertions for secure-window enabled/disabled launches. `mp-ui-designer-android` no-op; `mp-reviewer-android` passed production and test diffs; `mp-runner-android` passed focused `LockControllerTest`, `:app:assembleDebug`, and `:app:assembleDebugAndroidTest` (assembleDebugAndroidTest passed on retry after transient Windows file-lock contention); `mp-runner-instrumented-android` passed `MainActivityWindowSecurityTest` 2/2 on `Pixel_5_API_34` (SDK 34, boot complete). Manual task-switcher preview remains a checklist item because screenshots/task previews cannot be faithfully asserted from instrumentation. SPEC moved to `.claude/specs/done/`. Next backlog item: `audit3-lock-security-04-backup-rules-recovery.md`.
- **2026-06-12 (Codex MP feature --next, completed):** Completed SPEC `audit3-lock-security-02-pin-hardening`. Production commit `b1eefb8e` upgrades PIN hashes to `v2:<iterations>:<saltB64>:<dkB64>` with PBKDF2-SHA256 600,000 iterations, preserves legacy `saltB64:dkB64` verification at 10,000 iterations, lazily rehashes legacy PINs after successful unlock, persists PIN failure counters/deadlines in SecureStorage, and disables PIN keypad input during lockout countdowns with EN/RU retry text. Test commit `9654b480` updates `PinHasherTest`, adds JVM lockout/legacy-rehash coverage, updates the fake secure storage, and extends connected `SecureStorageTest` for lockout persistence. `mp-reviewer-android` passed; `mp-runner-android` passed `:feature:lockscreen:testDebugUnitTest --tests com.kshavrin.mymoney.feature.lockscreen.setup.PinHasherTest --tests com.kshavrin.mymoney.feature.lockscreen.overlay.LockOverlayTest`, `:feature:lockscreen:assembleDebug`, and `:app:assembleDebug`; `mp-runner-instrumented-android` passed connected `:core:datastore:connectedDebugAndroidTest` / `SecureStorageTest` 7/7 on `Pixel_5_API_34` (SDK 34, boot complete); `mp-verifier-android` passed with manual PIN-flow sanity checklist. `mp-docs` inert. The recorded TDD path `C:\Pet\MyMoney\TDD\MyMoney\MyMoney_TDD.md` was unavailable in this environment, so no TDD line-anchor re-read was claimed. `graphify update .` was launched by the post-commit hook. SPEC moved to `.claude/specs/done/`. Next backlog item: `audit3-lock-security-03-flag-secure-cold-start.md`.
- **2026-06-11 (Codex MP feature --next, completed):** Completed SPEC `audit3-lock-security-01-biometric-errors-pin-first`. Production commits `1e375fc0`, `b569850b`, `cc1f9b3d`, `a9fa41a5`, and `04842e7e` keep the lock overlay recoverable by routing biometric cancel/unavailable/error paths to PIN fallback or a localized retry-biometric legacy path, preserving `pinFallback` across restoration, launching `BiometricPrompt.authenticate()` on the activity UI thread, and preventing app lock from enabling until a PIN hash is present or newly written. `BiometricSetupViewModel` now cancels/dismisses pending PIN setup safely so a racing PIN write cannot re-enable lock after dismissal. Test and harness commits `5469cb23`, `12637049`, `45989cea`, `4a420789`, `f794a315`, `c4cbf703`, `368ef047`, and `75b5ac3f` add ViewModel regressions plus connected `LockOverlayUiTest` coverage for PIN fallback, legacy pinless retry, and saveable restoration, with a module Hilt runner/test manifest and deterministic biometric callback seam. `mp-ui-designer-android` no-op; `mp-reviewer-android` initially caught the setup dismissal race and hardcoded error color, then passed after fixes; final verifier passed. Device gate confirmed `Pixel_5_API_34` (SDK 34, boot complete). Verification passed with repo-local JDK 21: `:feature:lockscreen:testDebugUnitTest --tests com.kshavrin.mymoney.feature.lockscreen.setup.BiometricSetupViewModelTest :feature:lockscreen:assembleDebug :app:assembleDebug`, and connected `:feature:lockscreen:connectedDebugAndroidTest --tests com.kshavrin.mymoney.feature.lockscreen.overlay.LockOverlayUiTest` passed 3/3 through `scripts/run_connected_test_on_host_avd.ps1` with the required helper pause. SPEC moved to `.claude/specs/done/`. Next backlog item: `audit3-lock-security-02-pin-hardening.md`.
- **2026-06-11 (Codex MP feature --next, completed):** Completed SPEC `audit2-save-integrity-04-cancellation-rethrow-sweep`. Production commit `71e6185c` rethrows `CancellationException` before UI error-banner/Sentry mapping in Add Expense, Add Income, Transfer, Transaction Detail, and Search save/search paths; before sync failure remapping/Sentry in `SnapshotSyncRepository` and Dropbox `runOnIo`; and before WorkManager retry/failure mapping in BackupRotation, PruneDeleted, Recurring, and Sync workers. Test commit `eaffe213` adds cancellation-vs-real-failure coverage across the touched ViewModels, search debounce cancellation, sync repository/Dropbox behavior, and worker retry paths. Fix-up commit `7316d500` replaces a brittle private suspend reflection test with public Dropbox `accountLabel()` coverage and moves `SyncWorker` to depend on the `SnapshotSync` interface so worker cancellation can be tested at the correct boundary. `mp-developer-android` produced the production commit; `mp-reviewer-android` passed the production diff; `mp-tester-android` produced the regression tests; final reviewer spawning was blocked by the session usage cap, so the final review was completed locally with the same MP reviewer checklist. Verification passed with repo-local JDK 21: `:core:sync:testDebugUnitTest --tests com.kshavrin.mymoney.core.sync.SnapshotSyncRepositoryTest --tests com.kshavrin.mymoney.core.sync.dropbox.DropboxRepositoryTest --tests com.kshavrin.mymoney.core.sync.worker.WorkerCancellationBehaviorTest`, `:feature:transaction:testDebugUnitTest --tests com.kshavrin.mymoney.feature.transaction.expense.AddExpenseViewModelTest --tests com.kshavrin.mymoney.feature.transaction.income.AddIncomeViewModelTest --tests com.kshavrin.mymoney.feature.transaction.transfer.TransferViewModelTest`, `:feature:transactionslist:testDebugUnitTest --tests com.kshavrin.mymoney.feature.transactionslist.detail.TransactionDetailViewModelTest --tests com.kshavrin.mymoney.feature.transactionslist.search.SearchViewModelTest`, and `:app:assembleDebug`. An initial runner pass caught invalid Dropbox reflection tests and an inaccurate SyncWorker cancellation fixture; after repair, the focused gates passed. This was non-visual JVM/coroutine work, so no connected-device gate was required. `graphify update .` was launched by the post-commit hook. SPEC moved to `.claude/specs/done/`. Next backlog item: `audit3-lock-security-01-biometric-errors-pin-first.md`.
- **2026-06-11 (Codex MP feature --next, completed):** Completed SPEC `audit2-save-integrity-03-datastore-atomic-update`. Production commit `589b596d` moves `AppSettingsRepository.update` to compute the current `AppSettings`, run the pure transform, validate monotonic `firstPositiveSeen`, and write back inside one `dataStore.edit` critical section, removing the stale `settings.first()` pre-read that could lose concurrent settings writes. Test commit `b52d76cb` adds deterministic interleaving coverage for preserving a field committed immediately before edit application and for preventing account selection from resurrecting cleared CSV import focus fields; it also replaces the nested `runBlocking` monotonicity assertion with a suspend-native try/catch after the validation moved inside `edit`. `mp-developer-android` produced the production commit; `mp-reviewer-android` passed; tester/runner/verifier spawning was blocked by the session agent-thread cap, so test authoring and final verification were completed locally. Verification passed with repo-local JDK 21: `:core:datastore:testDebugUnitTest --tests com.kshavrin.mymoney.core.datastore.AppSettingsRepositoryTest` and `:app:assembleDebug`. This was non-visual data/JVM work, so no connected-device gate was required. `graphify update .` was launched by the post-commit hook. SPEC moved to `.claude/specs/done/`. Next backlog item: `audit2-save-integrity-04-cancellation-rethrow-sweep.md`.
- **2026-06-11 (Codex MP feature --next, completed):** Completed SPEC `audit2-save-integrity-02-double-tap-guards`. Production commits `ccfeb611` and `f359bc0` add synchronous `isSaving` check/set guards to Add Expense, Add Income, Transfer, Transaction Detail save/delete, and Account/Category/Currency/Goal edit ViewModels, with category-card save lookup failures reporting to Sentry and resetting `isSaving` so retry stays possible. Test commit `aef23782` adds blocking fake repository coverage for double `SaveClicked`/category-card/confirm-delete paths across the eight target ViewModels, asserting exactly one write and one navigation/action, plus error-reset coverage for category lookup failure. `mp-ui-designer-android` no-op; reviewer initially blocked the Add Expense/Add Income lookup error reset and passed after `f359bc0`; verifier passed. Verification passed with JDK 21: `:feature:transaction:testDebugUnitTest` for `AddExpenseViewModelTest`, `AddIncomeViewModelTest`, and `TransferViewModelTest`; `:feature:transactionslist:testDebugUnitTest` for `TransactionDetailViewModelTest`; `:feature:dictionaries:testDebugUnitTest` for `AccountEditViewModelTest`, `CategoryEditViewModelTest`, `CurrencyEditViewModelTest`, and `GoalEditViewModelTest`; and `:app:assembleDebug`. This was non-visual JVM ViewModel coverage, so no connected-device gate was required. `graphify update .` rebuilt the graph. SPEC moved to `.claude/specs/done/`. Next backlog item: `audit2-save-integrity-03-datastore-atomic-update.md`.
- **2026-06-11 (Codex MP feature --next, completed):** Completed SPEC `audit2-save-integrity-01-currency-rate-upsert-fix`. Production commit `2da61c02` fixes S27 Currency Rate saves for existing currency pairs by looking up `currencyRateRepository.findRate(fromId, toId)` inside `CurrencyRateViewModel.save()` and reusing `existing.id` before `upsert`, leaving DAO/entity/repository contracts unchanged. Test commit `749e21f6` adds module-local fake repository coverage for new-pair insert, existing-pair update with stored id reuse, and invalid-rate no-write error state. Reviewer passed; verifier passed. Verification passed with JDK 21: `:feature:transaction:testDebugUnitTest --tests com.kshavrin.mymoney.feature.transaction.rate.CurrencyRateViewModelTest`, `:app:assembleDebug`, and after clearing generated `feature/transaction/build/kspCaches` plus `--no-build-cache`, `:feature:transaction:testDebugUnitTest`. The first broad feature test attempt hit the known KSP incremental cache `file-to-id.tab already registered` infrastructure failure before tests executed; the clean-cache rerun passed. This was non-visual JVM coverage, so no connected-device gate was required. SPEC moved to `.claude/specs/done/`. Next backlog item: `audit2-save-integrity-02-double-tap-guards.md`.
- **2026-06-11 (Codex MP feature --next, completed):** Completed SPEC `audit1-timezone-04-tz-regression-suite`. Test commit `2bb8cc16` adds a shared `FixedTimeZoneRule` in `:core:testing`, New York Add Expense save/period/display regressions, Moscow Transaction Detail imported local-midnight round-trip coverage, and explicit `PeriodArithmetic` Day/Week/Month/CustomRange timezone boundary tests for UTC-minus and UTC-plus zones. Reviewer passed; focused verifier gates passed with repo-local JDK 21: `:core:domain:test --tests com.kshavrin.mymoney.core.domain.time.PeriodArithmeticTzTest`, `:feature:transaction:testDebugUnitTest --tests com.kshavrin.mymoney.feature.transaction.TimezoneRegressionTest`, `:feature:transactionslist:testDebugUnitTest --tests com.kshavrin.mymoney.feature.transactionslist.detail.TimezoneDetailRoundTripTest`, and `:app:assembleDebug`. This was test-only JVM coverage, so no visual/device gate was required. SPEC moved to `.claude/specs/done/`. Next backlog item: `audit2-save-integrity-01-currency-rate-upsert-fix.md`.
- **2026-06-11 (Codex MP feature --next, completed):** Completed SPEC `audit1-timezone-03-normalizer-migration`. Production commit `f2dfe68b` adds a one-time startup normalizer that runs from `MyMoneyApp` in `@ApplicationScope` on the injected IO dispatcher when `AppSettings.tzNormalizedAt == null`, shifts legacy UTC-midnight transaction instants to local midnight of the same UTC calendar day through `NormalizeLegacyUtcMidnightUseCase`, persists the guard flag only after success, and captures failures to Sentry so the next start retries. Repository/DAO support now lists normalization candidates and updates `occurredAt`/`updatedAt` in epoch millis, while `AppSettings` persists/removes `tzNormalizedAt`. Test commit `8d6c5c61` covers New York/Moscow/UTC/no-second-shift fixtures, repository epoch forwarding, and datastore guard round-trip/null clearing/reset. Reviewer passed; verifier passed with manual legacy-data checklist. Verification passed with JDK 21: `:core:database:testDebugUnitTest --tests com.kshavrin.mymoney.core.database.repository.TransactionRepositoryImplTest`, `:core:datastore:testDebugUnitTest --tests com.kshavrin.mymoney.core.datastore.AppSettingsRepositoryTest`, `:core:domain:test --tests com.kshavrin.mymoney.core.domain.usecase.NormalizeLegacyUtcMidnightUseCaseTest`, and `:app:assembleDebug`. The initially suggested `:core:domain:testDebugUnitTest` task does not exist, so the runner corrected to `:core:domain:test`. SPEC moved to `.claude/specs/done/`. Next backlog item: `audit1-timezone-04-tz-regression-suite.md`.
- **2026-06-11 (Codex MP feature --next, completed):** Completed SPEC `audit1-timezone-02-dashboard-picker-utc-boundary`. Production commit `dde6d7b7` routes dashboard Material3 picker UTC millis through a shared `materialPickerUtcMillisToLocalDate` helper in `PeriodStrip` and `LeftDrawerContent`, preserving AS-12 `CustomRange` semantics while fixing range and single-day picker boundaries in UTC-minus zones. Test commit `241af2c2` adds JVM timezone regression coverage pinned to `America/New_York`, a new `PeriodStripUiTest`, and timezone-pinned left-drawer picker coverage. Verification passed with repo-local/JBR JDK 21: `:feature:dashboard:testDebugUnitTest`, `:feature:dashboard:assembleDebug`, `:app:assembleDebug`, `:app:assembleDebugAndroidTest`; device gate confirmed `Pixel_5_API_34` (SDK 34, boot complete), then `PeriodStripUiTest` passed 1/1 and `LeftDrawerPeriodSelectorUiTest` passed 3/3 via `scripts/run_connected_test_on_host_avd.ps1` with the required pauses. `mp-ui-designer-android` no-op, reviewer sidecar passed, runner sidecar supplied the focused command set. SPEC moved to `.claude/specs/done/`. Next backlog item: `audit1-timezone-03-normalizer-migration.md`.
- **2026-06-11 (Codex MP feature --next, completed):** Completed SPEC `audit1-timezone-01-write-sites-local`. Production commit `715fca6e` changes transaction save/read date conversion from UTC midnight to `ZoneId.systemDefault()` local midnight in Add Expense, Add Income, Transfer, and Transaction Detail dirty/read/save paths while leaving Material picker UTC-millis boundary code untouched. Test commit `37b825f9` adds/pins timezone regression coverage for expense, income, transfer, transaction-detail save/read, and edit-form mapping with explicit `TimeZone` setup/restoration. Repo-local JDK 21 (`.gradle-local/jdks/eclipse_adoptium-21-amd64-windows.2`) cleared the previous environment blocker. `mp-runner-android` passed `:feature:transaction:testDebugUnitTest`, `:feature:transactionslist:testDebugUnitTest`, and `:app:assembleDebug`; `mp-verifier-android` passed with manual timezone checklist. `mp-reviewer-android` reported ambient pre-existing findings in touched files (rate preview string and comment-policy debt from older commits), but local blame confirmed they were not introduced by this SPEC. SPEC moved to `.claude/specs/done/`. Next backlog item: `audit1-timezone-02-dashboard-picker-utc-boundary.md`.
- **2026-06-08 (Codex MP feature --next, completed):** Completed SPEC `monefy-import-visibility-l10n-01-dashboard-surfaces-import`. Dashboard account/currency state is now reactive, imported accounts enter `DashboardSelection.AllAccounts` balance computation, and CSV import success records a one-shot DataStore import-focus signal (`importFocusEpochMs` / `importFocusCurrencyId`) so S01 jumps to the month and currency of the most recent imported row, then clears the signal. `importTransactionsCsv` now returns `CsvImportFocus?`; Monefy/MyMoney import paths derive focus from the most recent `occurredAt`, and Backup/Restore persists it before emitting CSV success. Production landed in `2aa0d7c3`; test coverage landed in `68980012`. `mp-ui-designer-android` returned no token changes; reviewer/tester sidecars supplied risk/test planning, but final reviewer/verifier spawning was blocked by the session agent-thread cap, so local review was used. Verification passed with repo-local JDK 21: `:core:datastore:testDebugUnitTest`, `:feature:dashboard:testDebugUnitTest`, `:feature:settings:testDebugUnitTest`, `:app:assembleDebug`, and `:core:database:assembleDebugAndroidTest`. `graphify update .` was not on PATH, but the post-commit graphify hook launched a background rebuild. SPEC moved to `.claude/specs/done/`. Next backlog item: `monefy-import-visibility-l10n-02-localize-seed.md`.
- **2026-06-08 (Claude bugfix, completed):** Fixed Monefy CSV import (`/mp --bugfix`). `importTransactionsCsv` now auto-detects header: Monefy (`date,account,category,amount,currency,converted amount,currency,description`) → new `importMonefyCsv` path; MyMoney header → unchanged existing path. `MonefyCsvImportParser` (pure JVM, 8 unit tests) handles U+00A0 grouping separator, comma-decimal, RFC-4180 quoting, signed amount→kind. `importMonefyCsv` auto-creates missing accounts (type=Cash, `ic_account_cash`) and categories (`ic_cat_other`) with exact CSV names (Russian as-is), additive merge inside one `withTransaction`. Real 8693-row Monefy export verified on Pixel_5_API_34 (emulator-5554, API 34) via new E2E instrumented test `MonefyCsvImportE2ETest` (1/1 green, 2.993 s). Commits: `d80718a8` (fix + parser + 8 JVM tests) + `ba29ac6b` (E2E test), merged and pushed to main.
- **2026-06-07 (Codex MP feature --next, no runnable SPEC):** `$mp --feature --next using subagents` found no active or runnable backlog SPEC. `.claude/specs/active/` contains only `.gitkeep`; `.claude/specs/backlog/` contained only stale `*-00-overview.md` epic indexes, which are ignored by the canonical `--next` rule. Subagent board audit confirmed the older "Next backlog item: `goal-monthly-contribution-breakdown-01-breakdown-domain-calc.md`" note was stale: the `goal-monthly-contribution-breakdown` epic is complete in `.claude/specs/done/`, and the `goals-credit-downpayment` device gap is closed. Moved completed overview indexes for `dashboard-donut-refinement` and `form-sticky-save-bar` to `.claude/specs/done/`, reconciled stale `Status:` headers for done-folder board files, and left app source/test files untouched. Next action: create/groom a new SPEC via `$mp --spec ...` or `$mp --plan ...` before running `$mp --feature --next` again.
- **2026-06-07 (Codex MP feature, completed):** Completed SPEC `form-sticky-save-bar-03-currency-rate-pinned-save`: the Currency Rate screen now uses the shared `FormBottomBar` in `Scaffold.bottomBar`, keeps the form body in the existing `verticalScroll` with `innerPadding`, and preserves `CurrencyRateEvent.SaveClicked`/`BackClicked` behavior and existing EN/RU save strings. Production landed in `ba5e7140`; focused connected Compose UI coverage landed in `1fedd37b`. `mp-ui-designer-android` returned no token changes; `mp-reviewer-android` passed; `mp-verifier-android` passed. Device gate confirmed `Pixel_5_API_34` (`emulator-5554`, SDK 34, boot complete). Verification passed `:feature:transaction:testDebugUnitTest`, `:feature:transaction:assembleDebug`, `:app:assembleDebug`, and `CurrencyRateScreenUiTest` 6/6 on `Pixel_5_API_34` via `mp-runner-instrumented-android` / `scripts/run_connected_test_on_host_avd.ps1` with the required pause. The broad `mp-runner-android` pass was blocked by unchanged repo-infra gaps (`:app:detekt` and `:app:jacocoUnitTestReport` missing, plus app-wide connected test plugin failure before instrumentation), so scoped gates were used. SPEC moved to `.claude/specs/done/`; the `form-sticky-save-bar` epic is complete. Next backlog item: `goal-monthly-contribution-breakdown-01-breakdown-domain-calc.md`.
- **2026-06-07 (Codex MP feature, completed):** Completed SPEC `form-sticky-save-bar-01-form-bottom-bar-component`: added a domain-free `FormBottomBar` foundation in `:core:designsystem` for future `Scaffold.bottomBar` Save buttons, using existing Material 3 `Surface`/`Button` styling and `Spacing.l`/`Spacing.m` padding without hardcoded user-facing text or screen wiring. Production landed in `89d1a8f2`; connected Compose UI coverage landed in `ec2f7ca5`. `mp-ui-designer-android` returned no token changes; `mp-reviewer-android` passed; tester/runner/verifier subagent spawns were blocked by the session agent-thread cap, so the small test and final verifier checks were completed locally. Device gate confirmed `Pixel_5_API_34` (`emulator-5554`, SDK 34, boot complete). Verification passed `:core:designsystem:testDebugUnitTest`, `:core:designsystem:assembleDebug`, `:app:assembleDebug`, and `FormBottomBarUiTest` 2/2 on `Pixel_5_API_34` via `scripts/run_connected_test_on_host_avd.ps1` with the required 60-second pause. SPEC moved to `.claude/specs/done/`. Next backlog item: `form-sticky-save-bar-02-dictionaries-edit-pinned-save.md`.
- **2026-06-07 (Codex MP feature, completed):** Completed SPEC `monefy-ux-fixes-06-donut-icon-tap`: populated dashboard donut category icon discs now share the slice tap path, so tapping a drawn category icon invokes `onSliceClick` for its mapped slice while ring taps and empty-state placeholder icon taps remain unchanged. The hit-test is limited to icons that are actually rendered, avoiding invisible zero-fraction targets. Production landed in `b6019804`, `28501013`, and `07cea942`; focused androidTest coverage landed in `9b8ba1e5`. Reviewer passed after the zero-fraction guard; verifier passed. Device gate confirmed `Pixel_5_API_34` (`emulator-5554`, SDK 34, boot complete). Verification passed `:core:designsystem:testDebugUnitTest`, `:core:designsystem:assembleDebug`, `:app:assembleDebug`, plus the two new `MonefyDonutChartUiTest` connected methods individually on `Pixel_5_API_34` via `scripts/run_connected_test_on_host_avd.ps1`. A full connected class attempt aborted on an unchanged earlier test with `INSTRUMENTATION_ABORTED: System has crashed` before the new tests ran; the targeted reruns were green with the required pauses. SPEC moved to `.claude/specs/done/`. Next backlog item: `monefy-ux-fixes-07-categories-scroll.md`.
- **2026-06-07 (Claude MP feature, completed):** Completed SPEC `monefy-ux-fixes-05-dashboard-negative-balance`: dashboard negative net balance now has dedicated theme tokens (`dashboardBalancePanelContainerNegative`, `dashboardBalancePanelContentNegative`, light+dark) and `DashboardBalancePanel` conditionally uses them when `state.balanceSnapshot.net.amount.signum() < 0`; positive and zero balances keep the existing panel tokens. Production landed across `c39caaea` (tokens) → `a9fa6c39` (token-only net repair) → `e75ec544` (conditional tint) → `f8725bf9` (tightened compose-ui test). This session resumed the prior Codex run that was blocked only on a device-infra flake (`ReferenceQueueDaemon`/`HardwareRenderer` GC-daemon timeout aborting an unrelated previously-green test at 3/36): committed the missing core-ui unit test in `05470d5a`; reviewer passed; verifier `pass:true`; `:core:ui:testDebugUnitTest` + `:feature:dashboard:testDebugUnitTest` green on repo-local JDK 21; device health preflight `HEALTH: OK` (hardware GPU rendering); connected `DashboardContentUiTest` re-ran clean **36/36, 0 failed** on `Pixel_5_API_34` via `scripts/run_connected_test_on_host_avd.ps1` — the flake gate is cleared (it was emulator-renderer degradation, not a logic defect). Pushed to `main`; SPEC moved to `.claude/specs/done/`. Next backlog item: `monefy-ux-fixes-06-donut-icon-tap.md`.
- **2026-06-06 (Codex MP feature):** Completed SPEC `monefy-ux-fixes-03-single-date-picker`: the shared Add Expense/Add Income transaction form date dialog now uses a Material3 single `DatePicker`/`rememberDatePickerState` via `TransactionDatePickerDialog`, preserves the `onDatePicked(LocalDate)` contract and UTC millis conversion, and leaves the dashboard AS-12 `DateRangePicker`/`Period.CustomRange` behavior untouched. Verification: visual device gate confirmed `Pixel_5_API_34` (`emulator-5554`, SDK 34, boot complete); `mp-ui-designer-android` made no token changes; `mp-developer-android` commit `61a50b9`; `mp-reviewer-android` passed; `mp-tester-android` updated dialog/expense/income Compose coverage in commit `0a6dd57`; `mp-runner-android` surfaced the known broad KSP/generated-dir cache issue, so scoped gates were rerun directly with repo-local JDK 21 and `--no-build-cache`; final gates passed `:feature:transaction:testDebugUnitTest`, `:feature:transaction:assembleDebug`, `:app:assembleDebug`, and 24/24 touched transaction Compose UI tests on `Pixel_5_API_34` via `scripts/run_connected_test_on_host_avd.ps1`; `mp-verifier-android` passed. The SPEC was moved to `.claude/specs/done/`; next backlog item is `monefy-ux-fixes-04-edit-uses-shared-form.md`.
- **2026-06-06 (Codex MP feature):** Completed SPEC `monefy-ux-fixes-02-form-layout-keypad-up`: the shared transaction form amount step now removes the empty vertical gap above the keypad, keeps the keypad directly below the note field, and lets the `Choose category` CTA expand into the freed lower space with the new `spacing.transactionFormChooseCategoryMinHeight` token while preserving amount-gated enablement and the category-step/BackToAmount flow. Verification: visual device gate confirmed `Pixel_5_API_34` (`emulator-5554`, SDK 34, boot complete); `mp-ui-designer-android` added token commit `9590e67`; `mp-developer-android` commit `a578380`; `mp-reviewer-android` passed; `mp-tester-android` added coverage and compile fix commit `a6439a7`; native runner fallback surfaced repo-missing `:app:detekt`/JaCoCo tasks and a Gradle cache metadata issue, so scoped gates were rerun directly with repo-local JDK 21 and `--no-build-cache`; final gates passed `:core:designsystem:testDebugUnitTest`, `:core:designsystem:assembleDebug`, `:app:assembleDebug`, and `TransactionFormContentUiTest` 6/6 on `Pixel_5_API_34` via `:core:designsystem:connectedDebugAndroidTest`; `mp-verifier-android` passed. The SPEC was moved to `.claude/specs/done/`; next backlog item is `monefy-ux-fixes-03-single-date-picker.md`.
- **2026-06-06 (Codex MP feature):** Completed SPEC `monefy-ux-fixes-01-extract-transaction-form`: extracted the shared transaction form foundation into domain-free `:core:designsystem` (`TransactionFormContent`, `TransactionFormState`, promoted `CategoryGrid` and `DateHeader`), rewired Add Expense/Add Income to map their domain-backed states into the shared form model, and updated Transfer to use the promoted `DateHeader` while preserving the existing keypad-first two-step flow and AS-4 add-category routing. Verification: visual device gate confirmed `Pixel_5_API_34` (`emulator-5554`, SDK 34, boot complete); `mp-ui-designer-android` no-op; `mp-developer-android` commit `50f7f0e`; `mp-reviewer-android` passed; `mp-tester-android` commit `27ebe13`; reviewer-blocked production test-tag shim `8a63bb2` was superseded by `082a587`, `13b624d`, and `500472e`; final scoped gate passed `:core:designsystem:testDebugUnitTest`, `:core:designsystem:assembleDebug`, `:feature:transaction:testDebugUnitTest`, `:feature:transaction:assembleDebug`, `:app:testDebugUnitTest`, and `:app:assembleDebug`; connected tests passed `TransactionFormContentUiTest` 4/4, `DateHeaderUiTest` 2/2, `CategoryGridUiTest` 4/4, `AddExpenseScreenUiTest` 11/11, and `AddIncomeScreenUiTest` 11/11 on `Pixel_5_API_34`; `mp-verifier-android` passed. The SPEC was moved to `.claude/specs/done/`; next backlog item is `monefy-ux-fixes-02-form-layout-keypad-up.md`.
- **2026-06-06 (Codex MP feature):** Completed SPEC `transactions-list-category-filter-clear`: records opened from dashboard/category navigation now treat `categoryId` as a real active category filter instead of a mere initial expansion hint, show a visible removable `Category: <name>` chip above the grouped list, limit loaded groups/transactions to that category, and clear back to the same account/currency/period records view in place when the chip is tapped. If the requested category is absent, the screen falls back to the unfiltered list and suppresses the chip. Verification: `mp-ui-designer-android` could not complete because of subagent usage quota, so no token changes were applied; local gates passed `:core:domain:test --tests com.kshavrin.mymoney.core.domain.usecase.GetCategoryRecordsUseCaseTest`, `:feature:transactionslist:testDebugUnitTest --tests com.kshavrin.mymoney.feature.transactionslist.list.TransactionsListViewModelTest --tests com.kshavrin.mymoney.feature.transactionslist.list.TransactionsListContentTest`, and `:app:assembleDebug`; `TransactionsListContentUiTest` passed 13/13 on `Pixel_5_API_34` via `scripts/run_connected_test_on_host_avd.ps1`. The SPEC was moved to `.claude/specs/done/`.
- **2026-06-06 (Codex MP feature):** Completed SPEC `dashboard-donut-refinement-03-icons-contour-no-disc`: populated dashboard donut category icons now render as bare slice-tinted outlines with no solid disc, sit on their own slice mid-angle just outside the ring with exploded offset applied, keep percentage labels outward on the same ray, and leave leader-line params dormant while preserving hit-testing, empty-state placement, budget badges, and AS-14's >=3% label gate. Verification: visual device gate confirmed `Pixel_5_API_34` (`emulator-5554`, SDK 34, boot complete); `mp-ui-designer-android` no-op; `mp-developer-android` commit `8a26168`; `mp-reviewer-android` passed; compile-surface fix `f817f6a` exposed the existing dashboard Other sentinel constants for app androidTest access; `mp-tester-android` added contour geometry and device pixel coverage, with stabilization commit `ac901dc` and JVM test commit `84ecb36`; JDK 21 gates passed `:core:designsystem:testDebugUnitTest`, `:core:designsystem:assembleDebug`, `:feature:dashboard:testDebugUnitTest`, and `:app:assembleDebug`; `mp-runner-instrumented-android` passed `MonefyDonutChartUiTest` 22/22 on `Pixel_5_API_34`; `mp-verifier-android` passed. The SPEC was moved to `.claude/specs/done/`; the `dashboard-donut-refinement` epic is complete.
- **2026-06-06 (Codex MP feature):** Completed SPEC `dashboard-donut-refinement-02-other-grouping`: dashboard donut slices below 2% of total expense now fold into one trailing display-only `Other` sentinel slice, localized at the Compose boundary, colored via `MaterialTheme.colorScheme.dashboardDonutOtherSlice`, excluded from budget alerts, and ignored by slice-click navigation while real category slices remain navigable. Verification: visual device gate confirmed `Pixel_5_API_34` (`emulator-5554`, SDK 34, boot complete); `mp-ui-designer-android` added token commit `7ad5b1a`; `mp-developer-android` commit `685c6d6`; `mp-reviewer-android` passed; `mp-tester-android` commit `c25aa91`; `mp-runner-android` passed `:feature:dashboard:testDebugUnitTest` and `:app:assembleDebug`; `mp-runner-instrumented-android` passed `DashboardContentUiTest` 32/32 on `Pixel_5_API_34`; `mp-verifier-android` passed. The SPEC was moved to `.claude/specs/done/`; next backlog item for this epic is `dashboard-donut-refinement-03-icons-contour-no-disc.md`.
- **2026-06-06 (Codex MP feature):** Completed SPEC `dashboard-donut-refinement-01-header-text-white`: dashboard top-bar title and currency subtitle now explicitly use `MaterialTheme.colorScheme.onPrimary`, keeping both texts white over the green header gradient in light theme while leaving period row, balance panel, FABs, and icon colors untouched. Verification: visual device gate confirmed `Pixel_5_API_34` (`emulator-5554`, SDK 34, boot complete); `mp-ui-designer-android` confirmed no new token was needed; `mp-developer-android` commit `dd23993`; `mp-reviewer-android` passed; `mp-tester-android` commit `8f041db`; `mp-runner-instrumented-android` passed `DashboardContentUiTest` 32/32 on `Pixel_5_API_34`; `mp-runner-android` passed `:feature:dashboard:testDebugUnitTest` and `:app:assembleDebug`; `mp-verifier-android` passed. The SPEC was moved to `.claude/specs/done/`; next backlog item for this epic is `dashboard-donut-refinement-02-other-grouping.md`.
- **2026-06-05 (Codex MP feature):** Completed SPEC `financial-goals-01b-loan-overpayment-semantics`: corrected `GoalLoanCalculator` to the locked financial-goals decision FG-4/BR-FG-7 "reduce payment, keep term" by adding `finalMonthlyPayment` and `interestSavedVsBaseline`, reporting `monthsToPayoff == termMonths` for overpayment, preserving the last non-zero declining payment when surplus zeroes the balance before the fixed horizon, and pinning the behavior with worked JVM fixtures. Verification: `mp-developer-android` commits `fc4923f` and `84e1763`; `mp-tester-android` coverage commit `153d3ca`; `mp-reviewer-android` passed after tightening the allowed hand-worked fixture comment; local JDK 21 gates passed `:core:domain:test --tests com.kshavrin.mymoney.core.domain.usecase.GoalLoanCalculatorTest` and `:core:domain:test`; `mp-runner-android` passed `:core:domain:test`; `mp-verifier-android` passed. The SPEC was moved to `.claude/specs/done/`; next financial-goals backlog item is `financial-goals-02-goal-persistence.md`.
- **2026-06-05 (Codex MP feature):** Completed SPEC `icon-library-expansion-04-category-icons-income`: added 16 income category icons to the shared `categoryIcon()` registry and `INCOME_ICON_KEYS`, bringing the category registry contract to 67 keys / 66 non-`other` vectors and adding income catalog order/resolution coverage. Verification: `mp-ui-designer-android` overreached into app theme token bootstrap commit `7b0c33d`, reverted by `8e6bafc`; `mp-developer-android` implemented the registry/catalog/test update in `5a85c5f`; `mp-reviewer-android` passed; `mp-tester-android` added income mapping/catalog coverage and `mp-developer-android` fixed the missing Material icon test imports in `2b33a27`; `mp-runner-android` passed with 49 tests / 0 failed and screenshots skipped; local JDK 21 gate passed `:core:designsystem:testDebugUnitTest`, `:feature:dictionaries:testDebugUnitTest`, and `:app:assembleDebug`; `mp-verifier-android` passed. The SPEC was moved to `.claude/specs/done/`; the `icon-library-expansion` epic is complete.
- **2026-06-05 (Codex MP feature):** Completed SPEC `icon-library-expansion-03-category-icons-expense`: added 34 expense category icons to the shared `categoryIcon()` registry and `EXPENSE_ICON_KEYS`, using Material Outlined glyphs where faithful plus custom stroke-only vectors for shoes and dentist; contract coverage now pins 51 category keys / 50 non-other vectors, representative new mappings, and expense catalog order/resolution. Verification: `mp-ui-designer-android` overreached into implementation commit `5803e85`; `mp-reviewer-android` passed; `mp-tester-android` added coverage in commit `be4effe`; `mp-runner-android` passed `:core:designsystem:testDebugUnitTest`, `:feature:dictionaries:testDebugUnitTest`, and `:app:assembleDebug`; local JDK 21 gate passed the same tasks; `mp-verifier-android` passed. The SPEC was moved to `.claude/specs/done/`; next backlog item for this epic is `icon-library-expansion-04-category-icons-income.md`.
- **2026-06-05 (Codex MP feature):** Completed SPEC `icon-library-expansion-02-icon-picker-renders-vectors`: `IconPickerSheet` now renders real `ImageVector` glyphs through an injected resolver while preserving key-based cell semantics, account/category edit screens pass the appropriate designsystem icon resolver, and account/category list circles render real icons instead of key abbreviations. Verification: device gate confirmed `Pixel_5_API_34` (`emulator-5554`, SDK 34, boot complete); `mp-ui-designer-android` no-op; `mp-developer-android` commit `1c107b9`; `mp-reviewer-android` recheck passed; `mp-tester-android` added coverage in commit `bcd2a75`; `mp-runner-instrumented-android` passed `IconPickerSheetUiTest` 2/2 and `AccountEditContentUiTest` 3/3 on `Pixel_5_API_34` after restarting the AVD to recover a device-service crash; local JDK 21 gate passed `:feature:dictionaries:assembleDebug :app:assembleDebug`; `mp-verifier-android` passed. The SPEC was moved to `.claude/specs/done/`; next backlog item for this epic is `icon-library-expansion-03-category-icons-expense.md`.
- **2026-06-05 (Codex MP feature):** Completed SPEC `dashboard-drawers-rework-04-hamburger-back-arrow`: when either dashboard drawer is open, the top-left nav icon now uses `Icons.AutoMirrored.Filled.ArrowBack`, exposes the new localized `dashboard_back` label, and dispatches `DashboardEvent.DrawerDismissed`; when drawers are closed it keeps the existing hamburger label/`LeftDrawerToggled` behavior. Verification: device gate confirmed `Pixel_5_API_34` (`emulator-5554`, SDK 34, boot complete); `mp-developer-android` commit `b707318`; `mp-tester-android` added coverage in commit `1237f5b`; `mp-runner-instrumented-android` passed `DashboardContentUiTest` 29/29 on `Pixel_5_API_34`; local gates with repo-local JDK 21 passed `:feature:dashboard:testDebugUnitTest :app:assembleDebug`; `mp-reviewer-android` recheck passed; `mp-verifier-android` passed. `mp-ui-designer-android` produced an out-of-scope token bootstrap commit `b6e0ae2`, immediately reverted by `1be50ee`. The SPEC was moved to `.claude/specs/done/`; the `dashboard-drawers-rework` epic is complete.
- **2026-06-05 (Codex MP feature):** Completed SPEC `dashboard-drawers-rework-03-scrim-and-back-dismiss`: dashboard drawer scrim taps and system Back now both dispatch the existing `DashboardEvent.DrawerDismissed` for left and right drawers, with `BackHandler` gated to open-drawer state so normal back propagation remains untouched when drawers are closed. Verification: `mp-ui-designer-android` no-op, `mp-developer-android` commit `11de4a5`, `mp-reviewer-android` pass after one capacity retry, `mp-tester-android` added coverage in commit `6d03dfc`; runner/verifier subagents were blocked by the session agent-thread cap, so local gates were run with JDK 21 and the documented host-AVD helper: `:feature:dashboard:testDebugUnitTest :app:assembleDebug`, `DashboardContentUiTest` 27/27 on `Pixel_5_API_34`, and `DashboardDrawerBackPressUiTest` 2/2 on `Pixel_5_API_34`. `graphify update .` reported no code-graph topology changes. The SPEC was moved to `.claude/specs/done/`; next backlog item for this epic is `dashboard-drawers-rework-04-hamburger-back-arrow.md`.
- **2026-06-05 (Codex MP feature):** Completed SPEC `dashboard-drawers-rework-02-right-drawer-from-right`: the dashboard overflow drawer now uses the custom overlay's right-side anchoring while the left drawer stays unchanged, and `DashboardContentUiTest` asserts the right panel hugs the screen's right edge while preserving the ~62% width. Verification green through MP subagents and project gates: reviewer pass, verifier pass, `:feature:dashboard:testDebugUnitTest`, `:app:assembleDebug`, and `DashboardContentUiTest` 25/25 on `Pixel_5_API_34` (`emulator-5554`) via `mp-runner-instrumented-android`. Commit: `7672b56`. The SPEC was moved to `.claude/specs/done/`; next backlog item for this epic is `dashboard-drawers-rework-03-scrim-and-back-dismiss.md`.
- **2026-06-05 (Codex MP feature):** Completed SPEC `dashboard-date-range-and-single-date`: the dashboard left drawer now labels the AS-12 two-date picker as **Date range**, keeps its `Period.CustomRange` behavior unchanged, and adds a new **Pick a date** row below it that opens a Material3 single `DatePicker` and emits `Period.Day(pickedDate)`. Verification green through MP subagents: reviewer pass, verifier pass, and `LeftDrawerPeriodSelectorUiTest` 3/3 on `Pixel_5_API_34` (`emulator-5554`) via `scripts/run_connected_test_on_host_avd.ps1`. Commits: `be41d9b`, `21f54b7`. The SPEC was moved to `.claude/specs/done/`.
- **2026-06-05 (Codex MP feature):** Implemented dashboard redesign visual target **38** from `docs/design/dashboard-redesign/dashboard-final-38-spec.md`. Added dashboard-specific theme tokens, moved the S01 balance into a compact tappable panel above the donut, upgraded the donut for equal-offset exploded slices/callouts/center divider, added period chevrons and labeled bottom actions, and replaced the Material top bar with a custom dashboard bar to prevent wordmark clipping. Verification green with JDK 21 from `.gradle-local`: `:feature:dashboard:testDebugUnitTest`, `:core:designsystem:testDebugUnitTest`, `:app:assembleDebug`, `DashboardContentUiTest` 25/25 on `Pixel_5_API_34`, and `MonefyDonutChartUiTest` 19/19 on `Pixel_5_API_34`. Visual screenshot: `build/visual-check/dashboard-final-38-dashboard-fixed.png`. Drawer side/dismiss review findings remain tracked separately in backlog SPECs `dashboard-drawers-rework-02-right-drawer-from-right.md` and `dashboard-drawers-rework-03-scrim-and-back-dismiss.md`.
- **2026-06-05 (Codex Product Design):** Finalized dashboard redesign visual target **38** for a future implementation session. The selected reference was copied to `docs/design/dashboard-redesign/dashboard-final-38.png`, and a full handoff spec was added at `docs/design/dashboard-redesign/dashboard-final-38-spec.md`. No production code was changed in this design-spec session.
- **2026-06-04 (Codex MP feature):** Completed SPEC `dashboard-drawers-rework-01-toolbar-visible-overlay`: dashboard drawers now render as custom in-content overlays below the `Scaffold` top bar, using dashboard drawer color/motion tokens, preserving the ~62% panel width, keeping toolbar actions visible/clickable while a drawer is open, and enforcing mutual drawer exclusion in `DashboardViewModel`. Verification green with JDK 21 from `.gradle-local`: `:feature:dashboard:testDebugUnitTest`, `:app:assembleDebug`, `DashboardContentUiTest` 19/19 on `Pixel_5_API_34`, and `DashboardDrawerOverlayUiTest` 3/3 on `Pixel_5_API_34`. The SPEC was moved to `.claude/specs/done/`; next backlog item for this epic is `dashboard-drawers-rework-02-right-drawer-from-right.md`.
- **2026-06-03 (Codex MP feature):** Completed the S01/S05 dashboard fidelity scale fix against reference `05.jpg`: the dashboard donut stage now expands through the middle viewport, chart side padding is reduced, the dashboard passes a larger donut radius fraction, and the two `-`/`+` FABs are 96 dp. Verification green: `:feature:dashboard:testDebugUnitTest`, `:app:assembleDebug`, `DashboardContentUiTest` 17/17 on `Pixel_5_API_34`, `MonefyDonutChartUiTest` 7/7 on `Pixel_5_API_34`. Visual screenshot: `build/visual-check/dashboard-fidelity-scale-after.png`.
- **2026-06-03 (Codex tooling):** Migrated Codex pipeline configuration from CMP-primary to MP Dev-primary using thin wrappers over Claude `mp-dev` 1.7.0. Added project skill `.agents/skills/mp-dev/`, native `.codex/agents/mp-*.toml` wrappers for all 18 MP Dev roles, and updated `AGENTS.md` to make `$mp` primary with `$cmp` legacy fallback. Static smoke and `$mp --check` simulation passed; live `mp-architect` spawn in this already-running Codex thread still reported `unknown agent_type 'mp-architect'`, so perform one new-session/runtime-reload smoke before deleting CMP files.
- **2026-06-02:** Plan complete → archived. The 15 phase files and `00_overview.md` were moved to `docs/implementation_plan/archive/` (`archive/phases/`, `archive/00_overview.md`). This PROGRESS.md and README.md stay here as the historical record.
- **2026-06-01 (Codex close-out):** Completed the implementation_plan by product decision: the remaining PHASE_15 release/manual gates are explicitly skipped for plan closure and deferred to release/v1.1 readiness. Deferred gates: signed clean-emulator R8 walkthrough (release keystore absent), manual accessibility/haptic/sound/dark-theme walkthrough, performance-budget tuning/reconciliation after macrobenchmark misses, and hosted CI green run after OQ-9 secret handling. Main implementation evidence remains green: 809/809 autotests, release build green, unsigned minified APK 12.38 MB, Baseline Profile generated, and macrobenchmark infrastructure executed.
- **2026-06-01 (Codex CMP):** Completed the PHASE_15 macrobenchmark/Baseline Profile infrastructure slice via native subagents. Reviewer blocked an initial `:core:database`/DAO benchmark-seeding approach; accepted commits `53ef6f4`, `9384fc1`, and `2db6be2` keep `:macrobenchmark` architecture-safe, generate `app/src/release/generated/baselineProfiles/baseline-prof.txt`, and run `StartupBenchmark` 3/3 green on `Pixel_5_API_34`. Device health was OK, but measured performance misses TDD budgets (cold-start median ~5.5 s, dashboard frame CPU p50 ~399 ms, list-scroll frame CPU p50 ~141 ms), so performance tuning/budget reconciliation remains open before release sign-off.

---

## Original `## Session log` entries from PROGRESS.md

> Entries below were moved verbatim from the old inline session log during the 2026-07-09 token-budget split.

2026-06-22 - BOARD category-neon-icons-refinement (`$mp --feature`, local/unpushed). Refined the PNG category tiles after visual review: all `category_neon_*.png` assets now have transparent outer corner pixels instead of black square edges, and the duplicate category-icon mappings were replaced with purpose-built neon bitmap tiles for the extra category keys while leaving the seeded/old category names untouched. The picker catalog now resolves 67 documented category keys to 67 distinct bitmap resources, with tests pinning no duplicate assets and representative non-reference mappings. Visual artifact: `build\icon-assets-preview\category-neon-contact-sheet.png`. Verified: PNG alpha check `all-corners-transparent`; `:core:designsystem:testDebugUnitTest`, `:feature:dictionaries:testDebugUnitTest`, `:core:designsystem:ktlintCheck`, `:feature:dictionaries:ktlintCheck`, `:app:ktlintCheck`, `:app:assembleDebug`; connected `NeonCategoryIconUiTest` 2/2, `IconPickerSheetUiTest` 2/2, and `CategoryEditContentUiTest` 5/5 passed on `Pixel_5_API_34`.
2026-06-22 - BOARD category-neon-icons-redo (`$mp --feature`, local/unpushed). Reworked category icon visuals from scratch after the reference-fidelity review: removed the previous custom vector icon catalogue from the category rendering path, generated 26 neon PNG tile assets directly from the supplied reference, added `CategoryIconAssets` mapping for all 67 documented category keys, and changed category pickers/grids to render the bitmap tiles instead of stylised legacy vectors. The first 26 expense-picker positions now follow the reference screenshot order; extra category keys reuse the same tile style through nearest-category mappings. `IconPickerSheet` now opens as a full-screen dark picker with 4-column 84dp tiles, no partial-sheet scrim and no selected border that would fight the reference art. Visual artifact: `build\visual-check\icon-picker-neon-reference-screen-4.png`. Verified: `:core:designsystem:testDebugUnitTest`, `:feature:dictionaries:testDebugUnitTest`, `:core:designsystem:ktlintCheck`, `:feature:dictionaries:ktlintCheck`, `:app:ktlintCheck`, `:app:assembleDebug`; connected `NeonCategoryIconUiTest` 2/2, `IconPickerSheetUiTest` 2/2, and `CategoryEditContentUiTest` 5/5 passed on `Pixel_5_API_34`.
2026-06-21 - BOARD category-icons-neon-redo via `$mp --feature`. Replaced the category icon catalog with locally authored neon stroke vectors for all 67 documented keys, including `ic_cat_other`, removing the previous Material-outlined glyphs from real category mappings. Enlarged the dictionaries icon-picker bottom sheet tiles/glyphs to match the supplied neon reference, kept unknown-key fallback as `Icons.Outlined.Category`, and updated catalog/UI tests to assert the new custom-vector contract and stable picker tags. Visual device gate passed on `Pixel_5_API_34` (`emulator-5554`, API 34, boot=1). Verified green: `:core:designsystem:testDebugUnitTest`, `:feature:dictionaries:testDebugUnitTest`, `:app:assembleDebug`, `:core:designsystem:ktlintCheck`, `:feature:dictionaries:ktlintCheck`, `:app:ktlintCheck`, `NeonCategoryIconUiTest` 2/2 connected, `IconPickerSheetUiTest` 2/2 connected, `CategoryEditContentUiTest` 5/5 connected. Work remains local and unpushed.
2026-06-01 - PHASE_15 - Completed task "Macrobenchmark" via native `$cmp --phase` subagents. Reviewer rejected direct `:core:database` benchmark seeding; accepted commits `53ef6f4` and `9384fc1` harden the UIAutomator launch journey without architecture violations, and `2db6be2` commits the generated release baseline profile. `BaselineProfileGenerator.generate` passed on `Pixel_5_API_34` and produced `app/src/release/generated/baselineProfiles/baseline-prof.txt`; `StartupBenchmark` passed `3/3` with `0` failed/skipped and device-health preflight OK, but measured cold-start/frame/list-scroll metrics missed TDD budgets, so performance tuning or budget reconciliation remains open.
2026-06-01 - PHASE_15 - Closed implementation_plan by product decision. Remaining PHASE_15 release/manual gates are explicitly skipped for plan closure and deferred to release/v1.1 readiness: signed clean-emulator R8 walkthrough (release keystore absent), manual accessibility/haptic/sound/dark-theme walkthrough, performance-budget tuning/reconciliation, and hosted CI green run after OQ-9. PHASE_15 now has 18/18 checklist items ticked and the phase table marks all phases done.
2026-06-07 - BOARD epic goals-credit-downpayment (3 SPECs, run as one unit, pushed to main 34101163..be13d999). Reworked credit goal (S29) to a two-phase model: phase 1 accumulate down payment (no interest, CEILING months), phase 2 annuity loan; principal = target - max(downPayment, startingCapital); removed FG-4 overpayment. SPEC01 GoalLoanCalculator/LoanProjection rewrite (commits ccd1d600+4a2263c5, 127 :core:domain tests green; loan totals derived from the HALF_UP-rounded monthly payment). SPEC02 persistence: Goal termDate->downPayment+termMonths, GoalEntity down_payment/term_months columns, DB v3->v4 MIGRATION_3_4 - REBASED from the SPEC's stale v2->3 because contribution-breakdown already shipped MIGRATION_2_3 (commit 46811914). SPEC03 form: down-payment field + loan-term-in-years input replacing the repayment-date picker + read-only year summary (commits 0a4c28cd+264a14ff). DEVIATION: VM kept Context-free (raw month ints in state, localized in the Composable) instead of the SPEC's @ApplicationContext-formatted strings, to preserve offline JVM testability. Test commit be13d999. DEFERRED: the v3->v4 migration + DAO round-trip + GoalsListContentUiTest are instrumented - compiled green but NOT yet executed on device (no device connected this session); run on Pixel_5_API_34 before release.
2026-06-07 - DEVICE verification of the goals-credit-downpayment epic on Pixel_5_API_34 (emulator-5554). Health-gate OK (hardware GL, smoke pass). :core:database:connectedDebugAndroidTest 19/19 green (MIGRATION_3_4 v3->4 + GoalDao downpayment/term round-trip + existing GoalDao suite, 0 failed). :app GoalsListContentUiTest 8/8 green (0 failed/skipped). The instrumented gap noted above is now CLOSED - all goal-credit instrumented suites pass on device.
2026-06-13 - BOARD epic audit5-donut-perf-02 (startup-and-measure, commit c1c3b81d, pushed to main). Moved cold-start hot-path work off the main thread without behaviour change: SoundPool 6-sound load deferred out of SoundPoolImpl ctor (background prefetch; play() before load = silence, no crash); SecureStorageImpl EncryptedSharedPreferences now `by lazy` over the audit3-04 recovery factory (Keystore off the MainActivity inject path); MyMoneyApp.onCreate scheduleDailyJobs() -> applicationScope.launch(ioDispatcher), audit1-03 normalizer launch preserved. Verified: :core:ui + :core:datastore + :app unit green; SecureStorageTest 11/11 on Pixel_5_API_34; clean :app:assembleDebug + install + launch smoke OK (no crash; RecurringWorker + PruneDeletedWorker ran SUCCESS - jobs still scheduled off-main). Reviewer pass, Verifier pass. DEFERRED: the :macrobenchmark before/after vs TDD budgets (G4) was NOT re-measured - release signing keys absent + x86_64 emulator (non-representative per the epic overview); needs a signed-release run on a physical ARM device. **Epic audit5-donut-perf COMPLETE (01+02 shipped to main).**
2026-06-22 - BOARD category-neon-png-tiles (`$mp --feature`, local/unpushed). Regenerated the extra category icons as full PNG neon tiles instead of the lower-quality custom-vector-like assets: removed the circular color-badge look, kept transparent outer corners, preserved the original/reference category assets and existing category keys, and kept the picker on the 67 distinct `category_neon_*.png` resources. Visual preview: `build/icon-assets-preview/category-neon-contact-sheet.png`. Verified on local/Pixel_5_API_34: alpha edge check (`67` PNGs, all corners transparent), `:core:designsystem:testDebugUnitTest`, `:feature:dictionaries:testDebugUnitTest`, `:core:designsystem:ktlintCheck`, `:feature:dictionaries:ktlintCheck`, `:app:ktlintCheck`, `:app:assembleDebug`, `NeonCategoryIconUiTest` 2/2, `IconPickerSheetUiTest` 2/2, and `CategoryEditContentUiTest` 5/5 green.
2026-06-22 - BOARD category-neon-png-tiles redo (`$mp --feature`, local/unpushed). Reworked the weak preview result after user feedback: discarded the postprocessed/blob-like extra-category PNGs and redrew all non-reference extra category assets from scratch as neon line-art PNG tiles with the same dark rounded-tile surface, colored glow, thin white core, and object details/dashes where appropriate. The original/reference category assets and category keys remain unchanged; only duplicate/new extra PNG artwork changed. Visual preview updated at `build/icon-assets-preview/category-neon-contact-sheet.png`. Verified: `Pixel_5_API_34` preflight (`emulator-5554`, SDK 34, boot complete), alpha edge check (`67` PNGs, all corners transparent), `:core:designsystem:testDebugUnitTest`, `:feature:dictionaries:testDebugUnitTest`, `:core:designsystem:ktlintCheck`, `:feature:dictionaries:ktlintCheck`, `:app:ktlintCheck`, `:app:assembleDebug`, `NeonCategoryIconUiTest` 2/2, `IconPickerSheetUiTest` 2/2, and `CategoryEditContentUiTest` 5/5 green.
2026-06-22 - BOARD dashboard-aurora-polish-01 (`$mp --feature --next`, local/unpushed). Completed SPEC `dashboard-aurora-polish-01-integer-amounts-and-currency-after`: dashboard Aurora balance, income/expense pills, and per-currency Aurora cards now truncate fractional money values toward zero with local `RoundingMode.DOWN`, display zero decimal digits, and keep the currency symbol after the number; shared `MoneyFormatter`, chart behavior, category tiles, and non-dashboard formatting were not changed. Commits: `e49af3dd` production and `b636ebee` tests. SPEC moved to `.claude/specs/done/`; overview leaves SPECs 02 and 03 queued. Verified: `Pixel_5_API_34` preflight (`emulator-5554`, SDK 34, boot complete), `mp-reviewer-android` pass, `:feature:dashboard:ktlintCheck`, `:app:compileDebugAndroidTestKotlin`, `:app:assembleDebug`, connected `AuroraBalanceCardUiTest` 11/11, connected `DashboardContentUiTest` 53/53 after recovering from one unrelated emulator crash/package-service loss, connected `CurrencyBalanceCardListUiTest` 18/18, and `mp-verifier-android` pass. `mp-docs` inert returned `{"committed": false}`.
```

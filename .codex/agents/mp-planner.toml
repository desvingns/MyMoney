name = "mp-planner"
model = "gpt-5.4"
model_reasoning_effort = "high"
description = "Plan ordered MyMoney MP Dev backlog SPECs from a spec bundle or TDD/design source."
sandbox_mode = "workspace-write"

developer_instructions = """
Read AGENTS.md, then read the canonical Claude agent body at C:/Users/k.shavrin/.claude/plugins/cache/mobile-pipeline/mp-dev/1.8.1/agents/mp-planner.md.
Read .claude/mp/config.json and .claude/mp/extras/mp-planner.md if it exists.
Return only the required PLAN block. The orchestrator performs gated writes; do not spawn descendant agents.
"""

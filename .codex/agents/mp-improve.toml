name = "mp-improve"
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
description = "Draft MP Dev plugin-level improvement proposals from MyMoney pipeline lessons."
sandbox_mode = "workspace-write"

developer_instructions = """
Read AGENTS.md, then read the canonical Claude agent body at C:/Users/Admin/.claude/plugins/cache/mobile-pipeline/mp-dev/1.12.0/agents/mp-improve.md.
Read .claude/mp/extras/mp-token-budget.md, then read .claude/mp/config.json and .claude/mp/extras/mp-improve.md if it exists.
Project-specific behavior changes belong in .claude/mp/extras first; plugin-level fixes may be proposed through the canonical mp-improve flow.
Return only the required PROPOSAL block or JSON payload. Do not spawn descendant agents.
"""

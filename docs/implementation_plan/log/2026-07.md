# Session log archive - 2026-07

> Entries below are verbatim moves from `docs/implementation_plan/PROGRESS.md` Current state during the 2026-07-09 token-budget split.
> Keep appending new history to the matching monthly file when compacting the head again.

---

- **2026-07-26 (Codex MP `--feature --next`, dependency-update radar):** Completed SPEC
  `review-2026-07-29-renovate-radar` in commits `1d2490e4`, `7c63a479`, `3f58cff2`,
  `30607c8f`, and `00f49b8a`. Added a monthly, grouped, report-only Renovate radar with
  Dependency Dashboard approval, labels, automerge disabled, and TDD-major-bump guidance.
  CI now keeps secrets out of every pull-request job, skips connected emulator tests only for
  authenticated `renovate[bot]` or the trusted `dependency-radar` label, and keeps JVM checks
  enabled. Feature contract tests passed 8/8; full parsed JVM evidence was 2912 passed with
  15 pre-existing Robolectric/Kover baseline failures; lint and ktlint were clean. Pushed to
  `origin/main` at `00f49b8a`.

- **2026-07-22 (Codex MP `--feature --next`, property-based tests):** Completed SPEC
  `review-2026-07-26-property-based-tests` in commits `21d9ac19`, `efcdc029`, `be8897d9`,
  and `0f697d18`. Added Kotest property coverage for operation merge ordering/tombstones,
  balance-trend series invariants, and backup rotation monotonicity, with fixed seeds and
  seed-on-failure reporting. `:core:domain:test` passed 398/0/0/0; feature-owned ktlint is
  clean. One pre-existing `FactoryResetUseCaseTest.kt` ktlint violation remains in the module
  baseline. Formal runner/verifier subagents were unavailable after the session usage limit;
  local ownership/static checks passed. No push performed.

- **2026-07-22 (Codex MP `--feature --next`, two-device journal merge E2E):** Completed SPEC
  `review-2026-07-25-two-device-merge-e2e` in commits `27fd43c5` and `1bb19fcf`. Added a
  deterministic Robolectric integration scenario with two in-memory Room databases, fixed device
  IDs, a fake shared-folder `JournalBackend`, same-time LWW conflicts, delete-vs-edit tombstones,
  category rename races, bidirectional exchange, full live-state comparison, and peer high-water
  assertions. Scoped JUnit XML is green (1/0/0/0); lint/detekt and reviewer, semantic, critic, and
  verifier gates passed. The generic JVM runner emitted its known no-summary/JaCoCo parser
  false-negative, cleared by XML. Push was attempted twice but timed out; the SPEC and closeout
  commits remain local ahead of `origin/main`.

- **2026-07-22 (Codex MP `--backlog`, private cloud sync):** Completed SPEC
  `review-2026-07-22-cloud-creds-setup`. Google Drive now uses only `drive.appdata` and app-owned
  `appDataFolder`; Dropbox remains App Folder; one provider/account binding is active at a time.
  Fresh real two-device Google and Dropbox round trips passed for `desving123456`, with persisted
  identity after restart and remote peer records in `outputs/e2e/`. Scoped JVM XML is green
  (sync 78/0/0, cloud-sync 4/0/0), the force-enabled debug APK built, and the Pixel 5/API 34
  Compose class passed 3/0/0. A runner safety incident uninstalled the user APK on Pixel 8/9 after
  AGP selected every healthy emulator; no remote journal was deleted, both local datasets were
  restored from Dropbox and verified, and `run_connected_test_on_host_avd.ps1` now refuses runs
  while non-gate devices are attached. No commit or push was made.

- **2026-07-16 (Codex MP `--feature --next`, type-safe navigation):** Completed SPEC `review-2026-07-19-type-safe-navigation`. Replaced string routes and `NavType` parsing with 29 `@Serializable` route types in `:core:ui`; `MyMoneyNavHost` uses typed `composable`/`navigate`/`popUpTo`, while all eight argument-owning ViewModels decode their shared route with `SavedStateHandle.toRoute<T>()`. Added `TypedNavigationDeviceTest`. Verification: deterministic reviewer, semantic review, independent critic, and full verifier passed; app JUnit XML 62/0/0/0 and targeted device XML 1/0/0/0 on local Pixel 5 / API 34. The generic JVM runner's no-summary/JaCoCo false negative was cleared by XML. Commits: `f7956d4c`, `b8ca1ab4`, `3dde2778`, `58d2b1fb`, `3b35a36a`, `59ebf5a`, `49c3b368`. Push is pending because noninteractive Git credentials are unavailable.

- **2026-07-16 (Codex MP `--feature --next`, backup privacy):** Completed SPEC `review-2026-07-17-backup-policy-decision`. Android system cloud backup now excludes the financial Room DB and its WAL sidecars, while Android 12+ device-to-device transfer preserves them; secure preferences and the `.bak` sidecar are excluded in every mode. Added ADR-0001 and the TDD-aligned sentinel-99 migration invariant. Targeted XML unit tests passed 5/5 with no failures/errors; Gradle exited 0 with lint/detekt green, while the generic runner's no-summary/JaCoCo parser false-negative was cleared by parsed JUnit XML. Deterministic reviewer, semantic review, independent critic, and full verifier passed. Commits: `f1a12bfd`, `fd12462c`, `a9282036`, `2bc83921`, `cb6c7fc5`.

- **2026-07-16 (Codex MP `--feature --next`, Aurora label contrast):** Completed SPEC `review-2026-07-16-aurora-balance-label-contrast` in commit `32dc598b`. Replaced the translucent `FREE BALANCE` label color with the existing semantic panel-content token. Fresh local Pixel 5 / API 34 evidence measured **6.70:1** against the Aurora panel (AA threshold 4.5:1); layout and semantics remained unchanged. JVM/static checks passed; the scoped connected class retained three unrelated baseline size/type assertions (26 passed / 3 failed). MP runner memory now authorizes automatic uninstall/reinstall of an incompatible MyMoney debug package on the local emulator without prompting. Pushed to `origin/main`.

- **2026-07-16 (Codex MP `--feature --next`, manual accessibility evidence):** Completed the manual contrast and TalkBack evidence pass for SPEC `review-2026-07-16-a11y-manual-pass`. Fresh screenshots/UI hierarchy dumps were captured on local `emulator-5554` / `Pixel_5` / SDK 34. The Aurora `FREE BALANCE` label measured **3.65:1** against its composited panel sample and is routed to the existing backlog SPEC `review-2026-07-16-aurora-balance-label-contrast`; all other sampled candidates passed 4.5:1. TalkBack remained enabled, but spoken announcements and accessibility-focus order were not verifiable through ADB, so the report records them as unverified. No production code or tests changed. Report: `docs/a11y/2026-07-15-manual-accessibility-pass.md`.
- **2026-07-15 (Codex MP `--feature --next`, dashboard trend-chart semantics):** Completed SPEC `review-2026-07-15-chart-semantics` in commits `9d51e58b`, `902f8d2c`, and `6ae9d50c`. Product decision is now explicit: the dashboard keeps its own `BalanceTrendChart` design; no Monefy-style donut is wired into production. The chart exposes a localized TalkBack summary with selected metric, period, start/end values, and direction; dashboard call sites supply the localized metric. Verification passed on local `emulator-5554` / `Pixel_5` / SDK 34: `BalanceTrendChartUiTest` 29/0/0/0, cleanup `MonefyDonutChartUiTest` 43/0/0/0, app JVM 104/0/0/0, lint/detekt, deterministic review, semantic review, and final verifier. The generic runner's no-summary parser false-negative was cleared by parsed XML. `graphify update .` extracted the AST but safely refused to overwrite an incomplete prior graph; no push performed.

- **2026-07-14 (Codex MP `--feature --next`, accessibility content-description audit):** Completed SPEC `review-2026-07-14-content-description-audit` in commits `f502f701`, `506f2a20`, `a1281e7e`, `49e0217b`, `4713350c`, `abb05f3c`, `7e39ff35`, `5d5e1eae`, `0986614e`, and `f3c0d219`. Audited actionable semantics across 10 production scopes, corrected localized descriptions and decorative-image semantics, preserved AS-12 interval/range-picker ownership, and added targeted Compose UI coverage. Compile, unit/build checks, and 49 targeted connected tests passed on local `emulator-5554` / `Pixel_5` / SDK 34; final semantic review and lite verifier passed. The full 421-test connected suite remains incomplete because unrelated baseline failures and an API 34 process crash occurred during the earlier broad run. Commit `190e9694` adds admission and verification-scope guards so future `--feature --next` sweeps are split before activation and broad connected runs are not started by default.
- **2026-07-14 (Codex MP bugfix, Compose 1.8 top-bar ATF gate):** Completed SPEC `review-2026-07-13c-topbar-title-atf-gate` in commits `f7cee65c`/`fcae16c2`. Root cause: `enableAccessibilityChecks()` installed a rule-wide validator but the class never triggered ATF with an action or `tryPerformAccessibilityChecks()`; the dormant validator perturbed all 22 layout tests and the accumulated device run stalled under UTP/resource pressure although individual methods passed. The test now uses a plain rule plus one isolated Week-state ATF testcase with explicit `onRoot().tryPerformAccessibilityChecks()`; explicit >=48dp assertions remain for menu, overflow, previous, and next controls. Deterministic reviewer and lite verifier passed. On local `emulator-5554` / `Pixel_5` / SDK 34, parsed XML is 23/0/0/0 and the prior stop at 18/22 is gone. The generic MP runner again emitted its known no-summary/JaCoCo parser false-negative despite Gradle exit 0; compile and connected XML cleared it. The durable lesson was appended to `.ai/memory/MEMORY.md` and queued to the brain inbox.
- **2026-07-12 (Codex MP `--feature --next`, repository fake contracts/dedup):** Completed SPEC `review-2026-07-11-fakes-dedup-contract-tests`. Contract safety net landed first in `7d695e15`; production commits `cc3cf28f`/`aaa752fa` add canonical `FakeAppSettingsRepository` and `FakeCurrencyRepository` under `:core:testing` with compatibility seams; test migration commit `178a75a0` points affected modules at the shared fakes. Ten superseded sources were preserved under git-ignored `archive/fakes-dedup-contract-tests/`. Verification: all affected JVM suites green, reviewer and verifier passed, and `CurrencyRepositoryImplContractTest` passed 4/4 with failures/errors/skips 0 on local `emulator-5554` / `Pixel_5` / SDK 34. Device diagnosis found the prior blocker was a stale exact-name/NAT-first preflight, not an emulator failure; `AGENTS.md`, helper messages, `.ai/memory/MEMORY.md`, and brain inbox now require local discovery first and accept `Pixel_5` or `Pixel_5_API_34` with SDK 34 + boot complete. No push performed.

- **2026-07-13 (Claude MP `--feature --next`, Roborazzi screenshot regression suite):** Completed SPEC `review-2026-07-12-roborazzi-screenshots`. Commit `86b2e81f` adds test-only Roborazzi 1.32.2 + Robolectric 4.14.1, applies the plugin to `:core:designsystem` and `:feature:dashboard` with `isIncludeAndroidResources=true`, and adds four `@Preview` entry points (donut, balance-trend, add-expense form, keypad) + a shared `ScreenshotTestHarness.captureThemed`. Commit `d9066241` adds `DesignSystemScreenshotTest` (donut/trend/keypad/add-expense amount+category, light+dark) and `DashboardScreenshotTest` (day-view, light+dark) with 12 committed baseline PNGs. Deterministic JVM/off-device render (fixed qualifier `w411dp-h914dp-xxhdpi`, seeded data, `snap()` anims) — no device or CI minutes. Verification: reviewer pass; both `recordRoborazziDebug` exit 0, `verifyRoborazziDebug` BUILD SUCCESSFUL, full `:core:designsystem` + `:feature:dashboard` unit suites green — the deterministic mp-runner reported the known kover/jacoco false-negative, cleared by this verified-manual pass; verifier `pass:true` (all six static checks n/a, test-only). Two mid-run session-limit resumes (developer, tester) were needed. Notes: "Aurora balance panel" from the epic index maps to no dedicated component → covered by the add-expense (transaction form) state; `RobolectricDeviceQualifiers` is absent in Robolectric 4.14.1 → pinned an explicit qualifier string. Pushed `027228a8..d9066241`.

- **2026-07-12 (Codex MP `--feature --next`, Kover coverage floors):** Completed SPEC `review-2026-07-10-kover-thresholds`. Production commit `986cb4b1` replaces symbolic coverage checks with measured per-module Kover line floors, excludes generated Hilt/Room/Android classes, and makes root `koverVerify` run every module verification; CI now generates and always uploads `kover-html-report`. Test commits `59e55a68`/`ce181cc5` add a four-case configuration contract. Baseline: domain 90.95% (90% gate), database 17.19% (17%), datastore 68.46% (68%), and feature modules 13.07%–46.29% with floors no more than one whole point below measurement; targets remain documented as future ratchets. Deterministic reviewer and native verifier passed; app XML is 104/0/0/0 including Kover contract 4/0/0/0; module reports match the baseline and Kover `verify.err` is empty. The generic MP runner reported a false red solely because its summary parser saw no aggregate Gradle line despite exit 0. No device run or push needed for this non-visual CI-only SPEC. `graphify query` returned unrelated stale graph nodes, so it was not used as verification evidence.
- **2026-07-11 (Codex MP `--feature --next`, CI connected-module coverage):** Completed SPEC `review-2026-07-09-ci-connected-modules` with native MP subagents. Public-repository visibility was confirmed, so existing push/PR triggers remain. Production commit `e4dddce3` adds `:core:sync`, `:core:network`, and `:feature:lockscreen` to the one Pixel 6 API 34 emulator run, each at 20 minutes and retaining `status=0` / `|| status=$?` aggregation. Test commit `2fc31437` adds `ConnectedModulesCiContractTest` for module order, timeouts, aggregation, and always-uploaded reports. Two deterministic reviews and the verifier passed; parsed focused XML was 2/0/0/0, `:app:ktlintCheck` and `git diff --check` passed. No device run or push was needed for this non-visual CI-only SPEC. `graphify update .` completed AST extraction.
- **2026-07-10 (Claude MP `--feature --next`, cold-start budget):** Completed SPEC `review-2026-07-07-cold-start-budget`. Production commits `5f43a63f` (convert `MyMoneyApp`'s five `@Inject lateinit` singletons and `MainActivity`'s sound/haptic players to `dagger.Lazy`, and move `SentryAndroid.init` inside `applicationScope.launch(ioDispatcher)`; `lockController` stays eager for the splash gate) and `aaa9ea91` (`matchingFallbacks += "release"` so `:app:assembleStaging` resolves library variants at all). Test commits `402ad667` and `e9905cea` pin the lazy-delegate contract and update the now-stale `SentryFreeTierContractTest` receiver assertion. Measured on `Pixel_5(AVD) - 14` via `:macrobenchmark:connectedBenchmarkReleaseAndroidTest`, `StartupBenchmark#coldStartupToDashboard`, 10 iterations: `timeToInitialDisplayMs` median `1199.3 ms` → `1101.3 ms` (-8.2%), max `1858.8 ms` → `1169.5 ms` (-37.1%). The SPEC's "~5.5 s" premise was a debug-build figure and is retired. `assets/dexopt/baseline.prof` verified present in `app-release.apk` and (newly) `app-staging.apk`. TDD §11's `<= 600 ms` budget targets a PHYSICAL Pixel 5 and stays UNVERIFIED — emulator numbers are only valid as a same-AVD delta. Reviewer passed 3x; `:app` unit tests 98/0/0/0; verifier `pass:true`. Pushed `2c1dc359..e9905cea`. **Two pre-existing reds surfaced and confirmed at parent `2c1dc359`, NOT introduced here:** `:feature:dictionaries:testDebugUnitTest` 283 tests / 5 failures (locale decimal-separator assertions, fallout of `review-2026-07-06`), and 9 ktlint violations in `app/src/test/.../L10nParityTest.kt` (from `657bec12`).

- **2026-07-10 (Codex MP `--feature --next`, locale-aware display formatting):** Completed SPEC `review-2026-07-06-locale-formatting-audit` with native MP subagents. Production commits `0f46565f` and `16520338` route user-visible money/rate/date values through locale-aware display formatting across the audited presentation surfaces and normalize localized comma-decimal rate input before numeric conversion; Room, JSON, and CSV formats remain invariant. Test commits `b71c7112` and `3c3805e8` add/update ru-RU regressions, including rate display-to-edit round-trip. Verification: deterministic reviewer passed twice; targeted module XML results total `1035` tests with 0 failures/errors/skips (including repair reruns `:feature:transaction` 97/0 and `:feature:transactionslist` 87/0); detekt/lint and `git diff --check` passed; verifier passed with all applicable static checks green. `graphify update .` completed its AST extraction. No device run was required for this unit-only non-visual SPEC; no push was performed.
- **2026-07-09 (Codex MP token-budget improvements):** Implemented the eight requested MP token-budget improvements in `e1bd0eb4`: split `PROGRESS.md` into a compact head plus verbatim monthly archives, added selected-mode runbook and context-capsule/token-budget rules, restored deterministic reviewer/runner preference, added deterministic connected-test wrapper `scripts/mp-runner-instrumented-android.ps1`, documented import/cold-start triage and same-agent repair loops, changed MP extras to role-based loading, and updated Codex MP agent/plugin paths to the installed personal `mp-dev` cache. Validation: PowerShell wrapper parsed successfully, `git diff --check` passed for touched files, and old `mp-dev` 1.8.1 paths are absent. SPEC `review-2026-07-08-progress-md-restructure` moved to `.claude/specs/done/`. No push was performed.
- **2026-07-09 (Codex MP `--feature --next`, import-focus cold start fixed):** Completed SPEC `review-2026-07-04-import-focus-cold-start-fix` with native MP subagents. Reproduced the known red `ImportFocusColdStartRegressionTest.importedRowsSurviveColdStartAndShowOnDashboard` on local fallback `emulator-5554` / `Pixel_5`, SDK 34, boot complete (initial XML: tests=1/failures=1/errors=0/skipped=0). Production commits `733b233b`, `fe5258c6`, and `42218b96` restore cold-start import focus for inactive imported currencies, prevent Monefy/MyMoney CSV import rows from colliding on blank UUIDs, and journal imported accounts/categories/transactions in the same import transaction so post-bootstrap sync invariants hold. Test commit `0fa8f392` adds an inactive-currency dashboard unit regression and makes the connected regression wait on the real dispatcher for the exact income+expense state it asserts. Verification: `mp-reviewer-android` passed after the journal repair; `:core:database:compileDebugKotlin`, `:core:database:compileDebugAndroidTestKotlin`, `:core:database:ktlintCheck`, `:feature:dashboard:testDebugUnitTest --tests com.kshavrin.mymoney.feature.dashboard.DashboardViewModelTest`, `:feature:dashboard:ktlintCheck`, and `:app:compileDebugAndroidTestKotlin` passed via MP runner; connected `ImportFocusColdStartRegressionTest` passed 1/1 with 0 failed/skipped on `Pixel_5(AVD) - 14`. SPEC moved to `.claude/specs/done/`; next runnable backlog item is `review-2026-07-05-missing-translation-gate`. No push was performed.
- **2026-07-09 (Codex MP `--feature --next`, CI signed AAB):** Completed SPEC `review-2026-07-03-ci-signed-aab` with native MP subagents. `gh repo view` confirmed `desvingns/MyMoney` is public, so the free-tier Actions constraint does not require removing existing push/PR coverage. Production commit `6914620b` splits CI into a per-run JVM job and a non-PR release packaging job, materializes release signing from GitHub secrets (`RELEASE_KEYSTORE_BASE64`, `RELEASE_KEYSTORE_PASSWORD`, `RELEASE_KEY_ALIAS`, `RELEASE_KEY_PASSWORD`), builds both `:app:assembleRelease` and `:app:bundleRelease`, and uploads signed APK/AAB artifacts; follow-up commit `8789f80f` fixes ktlint formatting. Test commit `bf2fead9` adds contract coverage for CI signing, artifact upload, public-repo triggers, and the existing Sentry free-tier CI injection. Verification: focused `:app:testDebugUnitTest --tests com.kshavrin.mymoney.SentryFreeTierContractTest --tests com.kshavrin.mymoney.ReleaseSigningCiContractTest`, `:app:ktlintCheck`, `:app:assembleDebug`, and `:app:assembleRelease` passed via MP runner; `mp-reviewer-android` and `mp-verifier-android` passed. No device run was needed for this non-visual CI/Gradle task. `graphify query/update` could not run because `graphify` is unavailable in PATH. No push was performed.
- **2026-07-09 (Codex MP `--feature --next`, S12 runtime route restored):** Completed active SPEC `review-2026-07-02d-transactions-list-runtime-route` with native subagent review. Production commit `20588d82` restores a runtime `Destinations.TRANSACTIONS_LIST` route, adds a dashboard operations-summary CTA into the full transactions list, and implements the S12 list route/screen/ViewModel in `:feature:transactionslist` without introducing feature-to-feature dependencies. Follow-up closeout guards mixed-currency `ConvertTo` summaries that cannot be faithfully represented by the S12 route by hiding/ignoring the CTA and covering the ViewModel + UI contracts. Device gate passed on local `emulator-5554` / `Pixel_5`, SDK 34, boot complete. Verification: `:app:testDebugUnitTest --tests com.kshavrin.mymoney.navigation.DestinationsTest`, `:feature:dashboard:testDebugUnitTest --tests com.kshavrin.mymoney.feature.dashboard.DashboardViewModelTest`, `:feature:transactionslist:testDebugUnitTest --tests com.kshavrin.mymoney.feature.transactionslist.list.TransactionsListViewModelTest`, `:app:compileDebugAndroidTestKotlin`, `:app:ktlintCheck`, `:feature:dashboard:ktlintCheck`, `:feature:transactionslist:ktlintCheck`, `:app:assembleDebug`, connected `OperationsSummarySheetUiTest` 6/6, and connected `TransactionsListRuntimeRouteTest` 1/1 passed on `Pixel_5(AVD) - 14` with failures=0/errors=0/skipped=0. The first connected attempt failed before tests because a signed release build was still installed (`INSTALL_FAILED_UPDATE_INCOMPATIBLE`); the rerun after package cleanup passed. No push was performed.
- **2026-07-08 (Codex MP `--feature --next`, Slice 5 release walkthrough completed):** Completed active SPEC `review-2026-07-02-slice5-release-qa` with native subagent assistance. Device gate passed on local `emulator-5554` / `Pixel_5`, SDK 34, boot complete; `scripts/preflight_device_health.ps1` was green with `MainActivityLaunchTest` 1/1. Signed `app/build/outputs/apk/release/app-release.apk` installed and cold-started (`TotalTime=631ms` on the final resume). Fresh release screenshots/XML under `build/visual-check/release-walk/` now cover S11, S01/S05, S02/S04, S03, S06/S07/S09, S08, S13-S27, plus S00 launch evidence. S27 was reached through a real release UI path by adding an EUR account and selecting it in Transfer. The only product defect found is S12 full transactions-list runtime reachability: `Destinations.TRANSACTIONS_LIST` is declared, but the current app graph has no route/UI entry for it; filed backlog SPEC `review-2026-07-02d-transactions-list-runtime-route`. No production/test code changes and no push were performed.
- **2026-07-08 (Codex MP `--feature --next`, Slice 5 release smoke advanced, walkthrough still open):** Resumed active SPEC `review-2026-07-02-slice5-release-qa` with native subagents. Device gate passed on local `emulator-5554` / `Pixel_5`, SDK 34, boot complete. `mp-runner-instrumented-android` first hit the expected release/debug signature mismatch after the signed release app was installed; after clearing package state, the sanctioned `MainActivityLaunchTest` rerun passed 1/1 with failures=0/errors=0/skipped=0 in `app/build/outputs/androidTest-results/connected/debug/TEST-Pixel_5(AVD) - 14-_app-.xml`. Reinstalled the signed `app-release.apk`, ran `pm clear`, cold-started `com.kshavrin.mymoney/.MainActivity` successfully (`TotalTime=1092ms`), and captured release screenshots/XML under `build/visual-check/release-walk/` for onboarding, dashboard, add expense, add income, transfer, date picker, left drawer, right drawer, and chart settings. No production/test code changes were made. Slice 5 remains active: signing is no longer the blocker, but the full 27-screen manual walkthrough/reporting is still incomplete.
- **2026-07-08 (Codex/user-assisted signing setup, signed release smoke green):** Release/staging signing was unblocked locally after the user generated `signing/mymoney-release.jks` and added the four git-ignored `local.properties` keys (`keystore.path`, `keystore.pass`, `keystore.key.alias`, `keystore.key.pass`). `:app:signingReport` now shows `release` and `staging` with `Config: release` and store `D:\Pet\MyMoney\signing\mymoney-release.jks`. `:app:assembleRelease` passed in 4m and produced signed `app/build/outputs/apk/release/app-release.apk` (18,092,135 bytes). Device smoke on local `emulator-5554` / `Pixel_5`, SDK 34, boot complete: installed the signed release APK, ran `pm clear`, cold-started `com.kshavrin.mymoney/.MainActivity` successfully (`TotalTime=781ms` after clear), and captured readable onboarding at `build/visual-check/mymoney-signed-release-after-splash.png`. Slice 5 is no longer blocked by signing; the remaining honest work is the broader manual 27-screen release walkthrough/reporting before closing the active SPEC.
- **2026-07-08 (Codex MP `--feature --next`, Slice 5 signing blocker reconfirmed):** Resumed active SPEC `review-2026-07-02-slice5-release-qa` after the macrobenchmark and onboarding contrast blockers were fixed. Device gate passed on local `emulator-5554` / `Pixel_5(AVD) - 14`: hardware GL, SDK 34, boot complete; `scripts/preflight_device_health.ps1` passed with `MainActivityLaunchTest` 1/1 in 51s. Re-ran `:app:signingReport`; ordinary `release` and `staging` remain unsigned (`Config: null`), while `nonMinifiedRelease`, `nonMinifiedStaging`, `benchmarkRelease`, and `benchmarkStaging` remain debug-signed. Slice 5 therefore remains active/blocked only on the clean signed-release walkthrough; no production/test code changes and no push were performed.
- **2026-07-08 (Codex MP `--feature --next`, Slice 5 onboarding contrast blocker fixed locally):** Closed follow-up SPEC `review-2026-07-02c-release-onboarding-contrast` with commits `39439425` and `9657d249`. `OnboardingSlide` now pins headline/body text to the dark onboarding surface content color, `OnboardingContentUiTest` adds a contrast regression that reads the real `TextLayoutResult` color and checks WCAG 4.5:1 contrast, and the release-style onboarding screenshot `build/visual-check/mymoney-release-onboarding-contrast.png` shows readable text. Verification: device gate passed on local `emulator-5554` / `Pixel_5(AVD) - 14`; `mp-reviewer-android` passed twice; connected `OnboardingContentUiTest` passed 6/6 in `app/build/outputs/androidTest-results/connected/debug/TEST-Pixel_5(AVD) - 14-_app-.xml`; `:feature:onboarding:ktlintCheck`, `:feature:onboarding:compileDebugKotlin`, and `:app:compileDebugAndroidTestKotlin` passed. Slice 5 remains active/blocked only on the ordinary signed-release walkthrough because `release`/`staging` signing config is still absent. No push was performed.
- **2026-07-08 (Codex MP `--feature --next`, Slice 5 macrobenchmark blocker fixed locally):** Closed follow-up SPEC `review-2026-07-02b-macrobenchmark-dashboard-probes` with commit `48d45218`. `BenchmarkJourney` now recognizes the redesigned dashboard signals (`FREE BALANCE`, money amount, income/expense stats, empty-period copy) and the current onboarding completion surface without changing production app behavior. Verification: device gate passed on local `emulator-5554` / `Pixel_5(AVD) - 14`; `:macrobenchmark:compileBenchmarkReleaseKotlin` passed; `:macrobenchmark:ktlintCheck` passed; connected `:macrobenchmark:connectedBenchmarkReleaseAndroidTest` reported `StartupBenchmark` 3/3 passed in `macrobenchmark/build/outputs/androidTest-results/connected/benchmarkRelease/TEST-Pixel_5(AVD) - 14-_macrobenchmark-.xml` (same suite has `BaselineProfileGenerator.generate` skipped); `mp-reviewer-android` pass. Slice 5 remains active/blocked on `review-2026-07-02c-release-onboarding-contrast` and the ordinary signed-release walkthrough. No push was performed.
- **2026-07-08 (Codex MP `--feature --next`, Slice 5 resumed/blocked with defects filed):** Resumed active SPEC `review-2026-07-02-slice5-release-qa`. Device gate passed on local `emulator-5554` / `Pixel_5(AVD) - 14`: hardware GL, SDK 34, boot complete; `scripts/preflight_device_health.ps1` passed with `MainActivityLaunchTest` 1/1. `:app:signingReport` still shows ordinary `release`/`staging` signing `Config: null`, but `benchmarkRelease`/`nonMinifiedRelease` are debug-signed. `:macrobenchmark:connectedBenchmarkReleaseAndroidTest` ran the minified `benchmarkRelease` target and failed `StartupBenchmark` 3/3 because `BenchmarkJourney` no longer recognizes the redesigned dashboard signals; filed follow-up SPEC `review-2026-07-02b-macrobenchmark-dashboard-probes`. A release-style onboarding smoke found near-black title/body text on the dark background; filed `review-2026-07-02c-release-onboarding-contrast`. Updated `docs/DEVICE_VERIFICATION_PROGRESS.md`; Slice 5 remains active/blocked until these defects and ordinary signed-release walkthrough are resolved. No push was performed.
- **2026-07-06 (Codex MP `--feature --next`, blocker fixed locally):** Continued active SPEC `review-2026-07-02-slice5-release-qa` with native MP subagents. Slice 5 had already moved to `.claude/specs/active/` and was blocked during device smoke by missing Hilt generated `MainActivityLaunchTest_TestComponentDataSupplier`; follow-up blocker SPEC `review-2026-07-02a-hilt-smoke-generated-supplier` was filed and completed. `app/build.gradle.kts` now enables Hilt's aggregating task so androidTest Hilt component data suppliers are generated/packageable; `scripts/run_connected_test_on_host_avd.ps1` accepts the current local `Pixel_5` AVD name when SDK is 34 and boot-complete. Verification: `mp-reviewer-android` pass; sanctioned connected smoke `scripts/run_connected_test_on_host_avd.ps1 -TestClass 'com.kshavrin.mymoney.MainActivityLaunchTest'` passed 1/1 on `Pixel_5(AVD) - 14` with XML failures=0/errors=0/skipped=0; `:app:assembleRelease` passed. `graphify update .` could not run because `graphify` is unavailable in PATH. SPEC `02a` moved to `.claude/specs/done/`; Slice 5 remains active/blocked on missing release signing for clean minified release install/walk and macrobenchmark. No push was performed.

- **2026-07-27 (Codex MP `--feature --next`, Monefy decoupling):** Completed SPEC
  `monefy-decoupling-02-database-rename-migration` in commits `910f1303`, `d9fc696b`,
  `7d7ae523`, `afa9d15f`, `6db6ff37`, `25e3e092`, `e5e157c8`, `6990f6d9`, `8c6d4a2a`,
  and `f25bc86c`. Room now migrates `monefy.db` plus `-shm`/`-wal` to `mymoney.db` before
  open, with marker-based restart recovery and checked backup replacement. JVM migration/XML
  tests, detekt, KtLint, Kover, and app JVM tests passed; connected SPEC-targeted backup tests
  passed 5/5 on Pixel 5 API 34. Real-device pre-existing rows survived the rename and the
  broad database connected suite compiled, while 27 unrelated legacy CSV/DAO/transfer tests
  remain red. SPEC and epic overview moved to `.claude/specs/done/`; push remains pending the
  human gate.

- **2026-07-26 (Codex MP `--feature --next`, tag-based versioning + CHANGELOG):** Completed SPEC
  `review-2026-07-30-versioning-changelog` in production commits `309230bb`, `965f6c8d`,
  `ff220440`, and `93035933`, with contract tests in `cf7336a2`. Git-derived stable SemVer
  versioning now uses a monotonic numeric scheme with offline fallback and release-task guards;
  CI validates full-history canonical tags; `CHANGELOG.md` is seeded at summary level. Local
  immutable tags are `v1.0.0`, `v1.0.1`, `v1.0.2`, and final `v1.0.3`. Reviewer and verifier
  passed; full `:app:testDebugUnitTest` passed after the final test-only repair. Deterministic
  runner's stale pre-repair 69/1 result is retained as a limitation; no push performed.
